# DECISIONS — 결정 기록 (ADR)

새 결정을 내릴 때마다 아래 형식으로 맨 아래에 추가한다. **검토했으나 버린 대안**을 반드시 남긴다 — 나중에 "왜 이렇게 했지?"를 다시 논의하지 않기 위함이다.

```
## [날짜] 제목

**결정**: 무엇을 하기로 했나
**이유**: 왜 이게 최선이었나
**버린 대안**: 검토했지만 쓰지 않은 방법과, 왜 안 썼는지
```

---

## 2026-07-28 백엔드 아키텍처 — 중앙 배포 Apps Script + GitHub Pages

**결정**: 프론트엔드는 React+Vite+TS로 GitHub Pages에 정적 배포. 백엔드는 운영자 계정에 Google Apps Script 웹앱을 **한 번만 배포**해 모든 교사가 공유. 데이터는 운영자 Drive(수업 JSON·미디어)와 Sheets(응답)에 저장.

**이유**: 별도 서버 구축이 요구사항에서 제외되어 있었고, 교사가 설정 없이 바로 쓸 수 있어야 했다. Apps Script는 무료이고 스프레드시트 자체가 "엑셀로 답안 수합" 요구를 자연스럽게 만족시킨다.

**버린 대안**:
- *교사별 개인 배포* — 개인정보가 교사 본인 계정에만 남아 가장 안전하지만, 교사마다 5분짜리 설정이 필요해 "교사가 설정 없이 바로 쓴다"는 목표와 충돌. 사용자가 중앙 배포를 선택.
- *Apps Script 단독 호스팅(HtmlService)* — 배포가 한 번으로 끝나지만 iframe 샌드박스 제약, 느린 로딩, 개발 편의성 저하로 기각.
- *Firebase(Firestore)* — 실시간 동기화엔 유리하나 별도 프로젝트 생성·요금제 확인이 필요해 "서버 구축 없이"라는 제약과 어긋나 기각.

## 2026-07-28 수식·화학식 입력 — MathLive, 버튼 전용, 자동변환 없음

**결정**: 수식 입력은 MathLive(`<math-field>`) + 커스텀 가상 키보드 5종(basic/fraction/greek/unit/chem)을 쓴다. 화학식은 별도 입력칸에 위/아래첨자 **토글 버튼**을 눌러 입력하며, `H2O → H₂O` 같은 자동 변환은 하지 않는다.

**이유**: 사용자가 "학생은 LaTeX 문법을 모른다고 가정해야 한다"고 명시적으로 지시했고, 화학식 첨자도 "자동변환보단 별도 버튼"으로 명시했다. MathLive는 시각 편집 + 커스터마이즈 가능한 버튼 키보드를 지원하면서 출력이 LaTeX 문자열이라 채점(`lib/grade.ts`)과의 연동이 쉽다.

**버린 대안**:
- *일반 텍스트 입력 + LaTeX 직접 타이핑* — 가장 구현이 쉽지만 사용자 제약과 정면으로 충돌해 기각.
- *H2O 자동 첨자 변환* — 입력 편의는 좋지만 사용자가 명시적으로 "자동변환보단 버튼"을 요청해 기각.
- *수식은 symbolic 비교만 지원* — `@cortex-js/compute-engine` 상시 로드는 번들이 무거워, 기본은 정규화 문자열 비교로 하고 symbolic은 옵션 문항에서만 동적 import 하기로 함.

## 2026-07-28 Node.js 설치 — winget 실패 → 사용자 폴더에 zip 압축 해제

**결정**: `winget install OpenJS.NodeJS.LTS`가 이 환경에서 동작하지 않아, Node.js 22 LTS의 Windows x64 **zip 배포판**을 직접 받아 `%LOCALAPPDATA%\Programs\nodejs`에 압축 해제하고, 사용자 PATH 환경변수(HKCU, 관리자 권한 불필요)에 등록했다.

**이유**: 확인해보니 (1) 이 셸에 `winget.exe`가 PATH에 없었고(App Installer 패키지는 등록돼 있으나 alias 미해결), (2) 현재 세션이 관리자 권한이 아니어서 공식 MSI 설치도 `1603`(치명적 오류, HKLM/Program Files 쓰기 거부로 추정)으로 실패했다. Node.js 공식 zip 배포판은 설치 프로그램 없이 그대로 실행 가능해 관리자 권한 문제를 완전히 우회한다.

**버린 대안**:
- *winget 재시도* — PATH에 실행 파일 자체가 없어 재시도로 해결될 문제가 아니었음.
- *MSI를 관리자 권한으로 재시도* — 이 자동화 환경은 UAC 대화형 승격을 처리할 수 없어(현재 세션 비관리자, 승격 시 사용자 확인 필요) 무인 설치가 불가능. 사용자에게 별도로 요청할 수도 있었지만, zip 방식이 사용자 개입 없이 즉시 해결 가능해 우선 적용.
- *nvm-windows 등 버전 관리자 설치* — 장기적으로는 더 편하지만, 그 설치 자체도 관리자 권한이 필요할 수 있어 이번엔 보류. 필요해지면 나중에 재검토.

## 2026-07-28 react-router-dom 취약점 경고 — 다운그레이드 없이 최신 유지

**결정**: 1단계 스캐폴딩 중 `npm audit`이 `react-router-dom@7.18.1`(설치된 최신 버전)을 high severity로 표시했다(`GHSA-qwww-vcr4-c8h2`, "RSC Mode CSRF Bypass Allows Action Execution Before 400 Response", 영향 범위 7.12.0~8.2.0). `npm audit fix --force`는 7.11.0으로 **다운그레이드**를 제안하지만 적용하지 않고 7.18.1을 그대로 쓰기로 했다.

**이유**: 이 취약점은 React Router의 **RSC(React Server Components) 모드**와 서버 loader/action 실행 경로에 한정된다. 이 프로젝트는 GitHub Pages에 배포되는 순수 정적 SPA이고 HashRouter로 클라이언트 사이드 라우팅만 하며, React Router의 데이터 API(loader/action)나 RSC는 애초에 쓰지 않는다. 따라서 이 벡터는 이 앱에 적용되지 않는다. 7 버전을 통째로 낮추면 이후 단계에서 다른 회귀를 만들 위험이 더 크다고 판단했다.

**버린 대안**:
- *`npm audit fix --force`로 7.11.0 다운그레이드* — 실제로 안전해지는 것 없이(우리는 취약 코드경로를 안 씀) 7개 마이너 버전의 개선사항을 잃는다.
- *react-router-dom을 아예 안 쓰고 직접 라우팅 구현* — 4개 뿐인 페이지라 가능은 하지만, 검증된 라이브러리를 버리고 직접 구현한 라우터의 버그 위험을 새로 떠안는 것이 더 나쁜 선택.

**주의**: 앞으로 이 프로젝트에 React Router의 loader/action이나 RSC 모드를 도입하는 결정을 내릴 일이 생기면, 이 취약점이 실제로 적용되는지 그때 다시 확인할 것.

## 2026-07-28 3단계 에디터 — 슬래시 메뉴는 "+" 버튼으로, 실시간 "/" 감지는 생략

**결정**: 블록 삽입 UI는 각 블록 hover 시 나타나는 "+" 버튼과 캔버스 하단의 "+ 블록 추가" 버튼으로 연다. Notion처럼 빈 문단에 "/"를 타이핑하면 바로 메뉴가 뜨는 실시간 키 입력 감지는 구현하지 않았다.

**이유**: "/" 감지를 제대로 하려면 특정 블록(TextBlock)의 TipTap 에디터 안에서 일어난 일을 상위 Canvas(블록 배열 자체를 바꿔야 함)로 끌어올려야 하는데, 이게 BlockEditorProps 계약(`onChange: (next: B) => void`, 같은 타입끼리만 교체 가능)을 깨거나 별도 콜백을 추가해야 해서 복잡도가 크게 늘어난다. 반면 "+" 버튼은 Notion 자신도 모바일 앱에서 기본으로 쓰는 방식이고, 이 프로젝트가 "모바일 환경에서 잘 작동"을 요구사항으로 명시했다는 점에서 오히려 터치 입력에 더 적합하다(모바일 키보드에서 "/"를 치는 것 자체가 번거로움).

**버린 대안**:
- *`@tiptap/suggestion`로 진짜 "/" 커맨드 구현* — 패키지 자체는 있지만, "다른 블록 타입으로 교체"라는 우리 아키텍처(블록마다 독립된 TipTap 인스턴스)에는 그대로 맞지 않아 상당한 추가 배선이 필요. 가치 대비 복잡도가 안 맞다고 판단해 보류. 나중에 사용자 피드백으로 꼭 필요해지면 재검토.

## 2026-07-28 3단계 에디터 — 되돌리기(undo/redo)는 구조적 변경만 기록

**결정**: 슬라이드·블록 추가/삭제/재정렬/보조슬라이드 지정 같은 "구조적 변경"만 `editorStore`의 되돌리기 스택에 쌓는다. 리치텍스트 타이핑, 문항 옵션 수정 같은 "내용 변경"(`updateBlock`)은 스택에 안 남긴다.

**이유**: 모든 키 입력마다 수업 전체 스냅샷을 되돌리기 스택에 쌓으면 (a) 스택이 몇 글자만 쳐도 금방 차고 (b) Ctrl+Z 한 번이 "글자 하나 지움" 수준이 되어 실제로 쓸모없는 되돌리기가 된다. 리치텍스트 타이핑 자체의 되돌리기는 TipTap/ProseMirror가 필드 단위로 이미 제공한다(각 텍스트 상자 안에서 Ctrl+Z 하면 그 상자의 타이핑 히스토리가 따로 동작).

**버린 대안**:
- *모든 변경을 스냅샷* — 위 이유로 기각.
- *디바운스해서 일정 시간마다 스냅샷* — 그래도 "무엇을 되돌리는지"가 사용자에게 예측 불가능해서 기각. 구조적 변경 단위가 더 명확하다.

## 2026-07-28 3단계 에디터 — 미리보기는 Viewer 나열, 우측 속성 패널은 생략

**결정**: `docs/PLAN.md`는 "미리보기"가 실제 플레이어 컴포넌트를 읽기 전용으로 재사용하고, 에디터에 "우 선택 블록 속성 패널"이 따로 있다고 적었다. 3단계에서는 둘 다 단순화했다: 미리보기는 현재 슬라이드의 Viewer 컴포넌트들을 그대로 나열만 하고(진행 잠금·다음 버튼 없음), 블록별 속성(이미지 폭, 영상 구간 등)은 별도 패널이 아니라 각 블록의 Editor 컴포넌트 안에 인라인으로 넣었다.

**이유**: 진짜 플레이어(진행 잠금, 다음/이전, feedbackMode)는 5단계 산출물이라 3단계 시점에는 아직 없다 — 지금 당장 쓸 수 있는 최소 버전으로 미리보기 가치를 먼저 제공하고, 5단계에서 `PreviewFrame`이 실제 플레이어를 읽기 전용으로 감싸는 형태로 교체한다. 속성 패널은 지금까지 블록 종류가 적어(8종) 인라인으로도 충분히 깔끔했고, 별도 패널을 만들면 "선택된 블록"이라는 새로운 상태와 레이아웃을 추가로 관리해야 해서 지금 단계의 이득 대비 복잡도가 안 맞았다.

**버린 대안**: 지금 당장 별도 속성 패널을 만드는 것 — 문항이 늘어나는 7~8단계에서 인라인이 너무 길어지면 그때 다시 판단한다(이 결정은 잠정적이며, `docs/PLAN.md`와 어긋난 부분이니 다음에 볼 때 재확인할 것).

## 2026-07-28 TipTap 확장 — `@tiptap/extension-color`/`font-family` 대신 `TextStyleKit` 사용

**결정**: 별도 `@tiptap/extension-color`, `@tiptap/extension-font-family` 패키지와 직접 만든 `FontSizeExtension.ts`를 쓰려다, `@tiptap/extension-text-style`가 이 버전(v3)에서 이미 `TextStyleKit`(Color+FontFamily+FontSize+BackgroundColor+LineHeight+TextStyle 번들)을 공식 제공한다는 걸 확인하고 그걸로 교체했다.

**이유**: 직접 만든 FontSize 확장이 공식 것과 커맨드 이름(`setFontSize`/`unsetFontSize`)까지 완전히 같아서 중복이었고, 패키지도 하나로 줄어든다. `lineHeight: false`로 안 쓰는 기능만 꺼서 씀.

**버린 대안**: 커스텀 `FontSizeExtension.ts` 유지 — 코드가 늘어나기만 하고 이점이 없어 삭제.

## 2026-07-28 4단계 연결형(match) 문항 — 드래그 연결선 대신 탭-투-페어

**결정**: `docs/PLAN.md`에는 연결형 문항의 정확한 인터랙션이 정해져 있지 않았다. 왼쪽↔오른쪽을 선으로 잇는 드래그 방식 대신, 왼쪽 항목을 탭하고 이어서 오른쪽 항목을 탭하면 짝이 완성되는 **탭-투-페어** 방식으로 구현했다.

**이유**: 이 프로젝트는 모바일 우선이 명시적 요구사항이다. 두 열 사이를 손가락으로 정확히 드래그해 선을 긋는 인터랙션은 작은 화면·터치 입력에서 오조작이 잦고 구현도 복잡한(SVG 선 그리기, 드래그 중 스크롤 처리 등) 반면, 탭-투-페어는 버튼 클릭 두 번으로 끝나 터치 정확도 요구가 낮고 dnd-kit 같은 별도 드래그 라이브러리도 필요 없다.

**버린 대안**: SVG로 선을 그리는 진짜 드래그 연결 — 시각적으로는 더 "연결형답게" 보이지만, 모바일 터치 정확도·구현 복잡도 대비 이득이 크지 않다고 판단. 데스크톱 사용성을 더 중시하게 되면 재검토.

## 2026-07-28 4단계 순서배열(order) 문항 — 학생 화면 셔플은 새로고침마다 다시 섞일 수 있음 (알려진 한계)

**결정**: 학생에게 보여줄 때 `question.items`를 `useMemo(() => shuffle(items), [question.id])`로 한 번만 섞고 그 이후 리렌더에는 유지한다. 하지만 학생이 페이지를 새로고침하면 컴포넌트가 새로 마운트되며 다시 섞인다 — 시드를 고정하지 않았다.

**이유**: 완벽히 고정하려면 `studentKey + questionId`를 시드로 하는 결정적 셔플이 필요한데, 지금 단계에서는 우선순위가 낮다고 판단해 미룬다. 실제로 문제가 되는 상황은 "학생이 순서를 절반쯤 맞추다가 새로고침"인데, 5단계(플레이어 자동저장)에서 `value`(학생이 만든 현재 순서)를 저장해두면 그 값이 있을 때는 셔플 결과 대신 저장된 순서를 그대로 쓰도록 이미 코드에 들어있다(`order.length === items.length`인 값이 있으면 그걸 우선) — 그래서 실제 영향은 "한 번도 안 건드린 상태로 새로고침하면 보이는 순서가 바뀐다" 정도로 작다.

**버린 대안**: 지금 당장 시드 고정 구현 — 5단계에서 `saveProgress` 연동을 마친 뒤 실제로 문제가 되는지 보고 재검토하기로 미룸(`docs/PROGRESS.md` 임시방편 목록에도 기록).

## 2026-07-28 5단계 플레이어 — 심각한 버그: 문항 지문(prompt)이 학생 화면에 전혀 안 보이고 있었다

**결정**: `src/player/SlideView.tsx`가 모든 문항 블록 공통으로 `question.prompt`를 살균해 렌더링하도록 고쳤다. 6종 문항 각각의 Viewer 컴포넌트는 고치지 않았다.

**이유**: 4단계에서 문항 6종의 Viewer를 만들 때, `QuestionEditorShell`(교사용 편집 화면)에는 지문 입력 RichTextEditor를 넣었지만, 학생용 Viewer 쪽에는 지문을 보여주는 코드를 어디에도 안 넣었다는 걸 5단계에서 실제로 플레이어를 켜보고서야 발견했다 — 답변 위젯(보기·입력칸 등)만 있고 정작 "무엇을 묻는지"가 안 보이는 상태였다. Viewer 6개를 각각 고치는 대신, 문항을 렌더링하는 공통 지점인 `SlideView.tsx` 한 곳에서 지문을 얹었다 — 앞으로 문항 유형이 늘어나도(7~8단계) 이 문제가 재발하지 않는다.

**버린 대안**: 6개 Viewer 컴포넌트 각각에 지문 렌더링 추가 — 중복 코드가 6곳에 생기고, 다음에 문항을 추가하는 사람이 또 깜빡할 수 있어 기각. 공통 렌더링 지점에서 한 번만 처리하는 쪽이 구조적으로 안전하다.

## 2026-07-28 5단계 API 확장 — `getProgress` 추가

**결정**: `docs/PLAN.md`의 Apps Script API 표에는 없던 `getProgress(code, studentKey)`를 `ApiClient`에 추가했다. 학생 응답을 studentKey로 조회만 하며 editToken은 필요 없다.

**이유**: PLAN.md 본문이 "새로고침·네트워크 끊김·**기기 변경** 시 같은 식별정보로 재입장하면 이어서 진행"을 학생 플레이어의 요구사항으로 명시하고 있는데, 기존 API 표에는 `saveProgress`(쓰기)만 있고 학생이 자기 응답을 다시 읽어올 방법이 없어 "기기 변경" 시나리오를 구현할 수 없었다. 자연스러운 누락으로 보고 그 자리에서 보충했다.

**버린 대안**: 없음 — PLAN.md에 이미 명시된 요구사항을 충족시키기 위한 필수 보충이라 별다른 대안 검토 없이 추가함.

## 2026-07-28 6단계 — Apps Script 백엔드를 직접 배포하지 않음

**결정**: `apps-script/Code.gs`·`appsscript.json`·`SETUP.md`를 전부 작성했지만, 실제로 script.google.com에 배포하고 웹앱 URL을 발급받는 절차는 수행하지 않았다. `docs/PROGRESS.md`에 "코드는 완료, 배포는 사용자 몫"이라고 명확히 기록해뒀다.

**이유**: Apps Script를 웹앱으로 배포하려면 Google 계정으로 로그인한 상태에서 **OAuth 권한 동의 화면("이 앱이 내 Drive/스프레드시트에 접근하도록 허용")을 직접 클릭**해야 한다. 이건 사용자 본인 계정에 실제 권한을 부여하는 행동이라, 자동화 도구가 대신 클릭하면 안 되는 범주(계정 인증·OAuth 동의는 항상 사용자 확인이 필요함)에 정확히 해당한다. GitHub 리포 생성(0단계)은 이미 대화 중 "GitHub 계정으로 로그인해서 대신 만들어달라"는 사용자의 명시적 지시가 있었지만, 이번엔 그런 지시가 없었고 무엇보다 계정 인증 동의라는 성격 자체가 다르다.

**버린 대안**:
- *claude-in-chrome으로 script.google.com에 접속해 전 과정을 대신 수행* — 이미 로그인된 세션이 있다면 기술적으로는 가능했겠지만, OAuth 동의 클릭이라는 경계선을 넘는 행동이라 하지 않았다.
- *배포 없이 이 문제를 아예 언급 안 하고 넘어가기* — 실사용 검증 없이 "완료"로 보고하면 사용자가 실제로는 동작을 확인 안 된 백엔드를 믿게 될 위험이 있어, PROGRESS.md와 최종 보고 양쪽에 명확히 남기기로 함.

## 2026-07-28 6단계 — Code.gs를 단일 파일로, LockService는 쓰기 경로에만

**결정**: `apps-script/Code.gs` 하나에 라우터·저장소·채점 로직을 전부 담았다(GAS는 파일을 여러 개로 나눠도 전역 스코프를 공유해 모듈 시스템이 없으므로, 복사·배포 편의를 위해 한 파일로 유지). `LockService.getScriptLock()`은 `createLesson`/`publishLesson`/`deleteLesson`/`saveProgress`/`submitResponse`에서만 쓰고, `getLesson`/`getResults`/`getAggregate`/`getProgress` 같은 읽기 전용 액션에는 걸지 않았다.

**이유**: 모든 요청에 스크립트 락을 걸면 학급 응답 집계 폴링(`getAggregate`, 9단계 예정)처럼 자주 호출되는 읽기 요청까지 전부 직렬화되어 불필요하게 느려진다. docs/PLAN.md도 "모든 **쓰기** 경로에서" 락을 걸라고 명시했지, 모든 요청이라고 하지 않았다.

**버린 대안**: 모든 액션에 락 적용 — 안전하긴 하지만 응답 속도가 눈에 띄게 나빠질 것으로 예상되어 기각.

## 2026-07-28 6단계 — 응답 스프레드시트의 문항별 열은 `_meta` 시트로 위치를 기억

**결정**: 학생 응답 스프레드시트는 `responses`(정식)/`_test`(테스트 모드)/`_meta`(문항ID→시작 열 매핑) 3개 시트로 구성했다. 새 문항이 응답에 처음 등장하면 `_meta`에 기록된 적 없는 questionId만 골라 `responses`와 `_test` 양쪽에 똑같은 위치로 열 3개(답/정오/점수)를 추가한다.

**이유**: 교사가 나중에 슬라이드에 문항을 추가·삭제해도 이미 쌓인 열이 밀리면 안 된다는 요구사항이 PLAN.md에 명시돼 있었다. 열 위치를 questionId 기준으로 고정해두면, 문항 순서가 바뀌어도 기존에 응답한 학생들의 열은 그대로 유지된다.

**버린 대안**: 매번 전체 헤더를 문항 순서대로 다시 씀 — 구현은 단순하지만 기존 응답 데이터의 열이 밀리는 문제를 그대로 안고 있어 기각.

## 2026-07-28 7단계 — numeric/chem/math 채점기를 `Code.gs`에 즉시 이식

**결정**: `src/lib/units.ts`/`sigfigs.ts`/`numericInput.ts`/`chemNormalize.ts`/`mathNormalize.ts`의 로직을 `apps-script/Code.gs`에 거의 그대로(함수 시그니처·분기 구조 동일하게) 옮겨 적어 `GRADERS` 맵에 `numeric`/`chem`/`math`를 추가했다. 브라우저로 세 유형을 채점까지 검증한 직후, 다음 단계로 넘어가기 전에 바로 이 작업을 했다.

**이유**: 6단계에서 이미 "GAS와 TS 채점 로직은 자동으로 동기화되지 않는다"는 위험을 문서화해뒀는데(`docs/PROGRESS.md` 임시방편 목록), 그 위험이 실제로 발생하는 첫 시점이 7단계였다. 미루면 다음 세션에서 "이미 다 됐다고 생각했는데 실제 배포에서는 안 됨"이라는 형태로 재발견하게 될 게 뻔해서, TS 쪽 로직이 완전히 검증된 직후 곧바로 이식했다.

**버린 대안**:
- *공용 로직을 별도 파일로 뽑아 GAS/TS 양쪽에서 import* — Apps Script는 npm 모듈을 못 쓰고 `clasp` 같은 빌드 파이프라인을 새로 들여야 해서, 이 프로젝트 규모에 비해 과잉 구성이라 기각(기존 6단계 결정과 같은 이유).
- *symbolic 수식 비교(Compute Engine)까지 GAS에 이식* — 프런트엔드 자체가 아직 `symbolic` 모드를 구현 안 했으므로(동적 import 예정, 미구현) GAS에서도 `normalized` 비교만 이식했다. symbolic이 나중에 TS 쪽에 추가되면 그때 GAS도 같이 갱신.

## 2026-07-28 8단계 — 그리기·사진 문항은 `grade`를 아예 등록하지 않음 (선택적 필드로 변경)

**결정**: `blocks/questions/types.ts`의 `QuestionDefinition.grade`를 필수에서 선택적 필드로 바꾸고, `registry.ts`의 `registerQuestion`이 `grade`가 있을 때만 `lib/grade.ts`에 채점기를 등록하도록 고쳤다. `kind:'drawing'`/`kind:'photo'`는 아예 `grade`를 넘기지 않는다.

**이유**: 두 유형 모두 애초에 정답(`answer`) 필드 자체가 타입에 없다 — 교사가 "정답"을 설정할 방법이 없으므로 자동채점이 원천적으로 불가능하다. 만약 다른 서답형 문항처럼 항상 `{correct:false, points:0}`을 반환하는 `grade`를 등록했다면, `submitResponse`가 이걸 그대로 `scores`에 저장해서 (a) `feedbackMode:'immediate'`나 `'onFinish'`에서 학생에게 "✗ 오답" 배너가 뜨고 (b) 요약 화면의 총점 계산에도 반영돼 실제로 아직 교사가 보지도 않은 답안을 "틀렸다"고 잘못 알리는 문제가 생긴다. `lib/grade.ts`는 원래부터 "그레이더가 없으면 null, null과 '채점했지만 틀림'을 구분한다"는 주석을 갖고 있었는데(6단계 작성 당시엔 실제로 이 경로를 타는 유형이 없었다), 이번에 그 설계를 실제로 쓰게 됐다. 브라우저로 검증했을 때도 데이터표만 점수에 반영되고 그리기·사진은 총점 계산에서 아예 빠지는 것을 확인했다.

**버린 대안**:
- *`grade`를 필수로 유지하고 항상 `{correct:false, points:0}` 반환* — 처음에 이렇게 구현했다가, 학생에게 오해를 주는 문제를 깨닫고 되돌렸다.
- *`GradeResult`에 `'ungraded'` 같은 세 번째 상태 추가* — 가능한 설계지만, 이미 `gradeQuestion`이 `null`로 "채점 안 함"을 표현하는 메커니즘을 갖고 있어서 `grade`를 선택적으로 만드는 쪽이 기존 설계와 더 잘 맞고 변경 범위도 작다.

## 2026-07-28 8단계 — 데이터표 계산 열 파서: `Object.hasOwn`으로 프로토타입 체인 우회

**결정**: `lib/formula.ts`의 열 참조 평가에서 `ctx.row[name]`을 바로 읽지 않고 `Object.hasOwn(ctx.row, name)`으로 소유 속성인지 먼저 확인한다.

**이유**: 테스트를 작성하다가 `evaluateFormulaString('__proto__', { row: {}, columns: {} })`가 오류를 던지지 않고 `Object.prototype`을 반환하는 걸 발견했다 — 일반 객체에서 `obj['__proto__']`는 `undefined`가 아니라 실제 프로토타입 객체를 돌려주는 접근자이기 때문이다. 코드 실행으로 이어지진 않지만(애초에 `eval`을 안 쓰므로), 정의 안 된 열을 참조했을 때 오류 대신 조용히 이상한 비-숫자 값이 계산에 섞여 들어갈 수 있는 실제 버그였다. `Object.hasOwn`으로 소유 속성만 걸러 정확히 "정의되지 않은 열"로 처리되게 고쳤다.

**버린 대안**: `__proto__`/`constructor` 같은 이름을 블록리스트로 걸러내는 방식 — 이름 목록을 유지보수해야 하고 다른 우회 경로(예: `toString`, `valueOf`)를 놓칠 위험이 있어, `hasOwn` 하나로 일반화해 해결하는 쪽을 택했다.

## 2026-07-28 9단계 — 보조 슬라이드는 "보통 진행"에서 자동으로 건너뛴다

**결정**: `lib/navigate.ts`의 "분기 규칙이 안 맞을 때 가는 기본 목적지"는 단순히 `slides[index+1]`이 아니라, 메인 슬라이드에서는 바로 뒤에 이어지는 보조 슬라이드(`isSub:true`)들을 전부 건너뛰고 다음 메인 슬라이드로 간다. 보조 슬라이드 자기 자신의 기본 진행은 배열상 다음 슬라이드로 그대로 이어간다(다음 보조 슬라이드 또는 다음 메인 슬라이드).

**이유**: 처음에는 단순히 `index+1`로 구현했는데, `validateBranchGraph`(도달 불가 슬라이드 검사) 테스트를 작성하다가 논리적 모순을 발견했다 — 모든 슬라이드가 배열상 다음 슬라이드로의 엣지를 항상 갖는다면, 슬라이드 0에서 시작해 그 체인만 따라가도 모든 슬라이드가 항상 "도달 가능"해져 버려서 도달불가 검사 자체가 의미 없어진다. docs/PLAN.md의 원래 의도("보조 슬라이드는 분기 규칙으로만 도달해야 하는 슬라이드")를 살리려면, 정상 진행(분기 규칙이 없거나 안 맞을 때)은 보조 슬라이드를 건너뛰어야 한다 — 그래야 "이 보조 슬라이드를 가리키는 분기 규칙이 하나도 없으면 실제로 아무도 그 슬라이드에 못 간다"는, 교사에게 진짜로 유용한 경고가 나온다.

**버린 대안**: 보조 슬라이드 여부와 무관하게 항상 `index+1`을 기본값으로 — 구현은 단순하지만 위에서 설명한 대로 도달불가 검사가 무력화돼 기각.

## 2026-07-28 9단계 — 플레이어 네비게이션을 `slideIndex`(숫자)에서 `path`(방문한 슬라이드 id 배열)로 전환

**결정**: `Player.tsx`의 상태를 숫자 인덱스 하나 대신, 실제로 방문한 슬라이드 id들의 배열(`path`)로 관리한다. "다음"은 `resolveNextSlideId`로 구한 id를 배열 끝에 추가하고, "이전"은 배열 마지막 원소를 pop한다.

**이유**: 조건 분기가 생기면 "다음 슬라이드"가 배열 순서와 다를 수 있다(정답이면 5번, 오답이면 4-1번). 숫자 인덱스만으로는 "뒤로 가기"가 실제로 학생이 지나온 경로를 되짚어줄 수 없다 — 예를 들어 4번에서 오답으로 4-1로 갔는데 "이전"을 누르면 인덱스 기반으로는 배열상 4-1 앞(즉 4번)으로 가지만, 만약 다른 경로였다면 틀린 슬라이드로 돌아갈 수 있다. `path` 배열은 실제 이동 이력이므로 되돌아가기가 항상 정확하다. 응답 기록의 `ResponseRecord.path`(엑셀 "진행경로" 열)도 원래 이 배열 그대로 쓰는 필드라 자연스럽게 재사용된다.

**버린 대안**: `slideIndex` 유지 + 별도 `history: number[]` 스택 추가 — 상태가 두 개로 늘어나고 서로 동기화해야 해서, 애초에 하나(`path`)로 통일하는 쪽이 더 단순했다.

## 2026-07-28 9단계 — POE 잠금을 서버에서도 강제(`enforceLocks`)

**결정**: 클라이언트가 `lockAfterSubmit` 문항을 잠갔다는 사실(`lockedQuestionIds`)을 `ResponseRecord`에 실어 서버로 보내면, 서버(`mock.ts`/`Code.gs` 둘 다)는 이후 같은 학생의 `saveProgress`/`submitResponse` 요청에서 이미 잠긴 문항의 답 값을 클라이언트가 보낸 값으로 절대 덮어쓰지 않고 이전 저장값을 유지한다. 잠금 목록 자체도 합집합으로만 늘어나 클라이언트가 목록에서 빼서 보내도 무시된다.

**이유**: docs/PLAN.md가 "서버도 lockedAt이 찍힌 예측 답안의 재수정을 거부한다(개발자도구로 우회 방지)"를 명시적으로 요구했다. 클라이언트 쪽 UI 비활성화만으로는 브라우저 개발자도구로 `fetch`를 직접 호출해 우회할 수 있어 의미가 없다.

**버린 대안**: `lockedAt` 타임스탬프를 문항별로 저장해 "제출 시각 이후의 수정 요청"만 거부 — `lockedQuestionIds` 목록에 있는지만 보는 것보다 정교하지만, 이 프로젝트 규모에서 시각 비교까지는 과잉이라 판단해 간단한 목록 방식을 택했다.

## 2026-07-28 9단계 — `poeGroup` 블록은 화면에 아무것도 그리지 않는다 (메타데이터 전용)

**결정**: `blocks/PoeGroup.tsx`의 학생용 Viewer는 `null`을 반환한다. 예측·설명 문항 자체는 평소처럼 별도 문항 블록으로 슬라이드에 추가하고, `poeGroup` 블록은 그 둘의 id(`predictId`/`explainId`)만 기록해 (1) `SummaryView`가 "내 예측 vs 내 설명"을 짝지어 보여줄 때, (2) 앞으로 POE 관련 기능을 더 얹을 때 참조할 메타데이터로만 쓴다.

**이유**: 예측 문항의 실제 잠금 동작은 `poeGroup` 블록의 존재 여부와 무관하게 문항 자신의 `lockAfterSubmit` 필드 하나로 이미 완결된다. `poeGroup`이 예측/설명 블록을 대신 렌더링하면 같은 문항이 두 번(원래 위치 + poeGroup 안) 그려지는 문제를 피하기 위해 별도 렌더 로직과 "원래 위치의 블록은 숨긴다"는 예외 처리가 필요해진다 — 렌더링은 각 블록이 자기 자리에서 하고, `poeGroup`은 오직 "이 둘은 짝이다"라는 사실만 기록하는 쪽이 훨씬 단순하다.

**버린 대안**: `poeGroup`이 예측→관찰→설명을 하나의 시각적 카드로 묶어 렌더링 — 더 "그룹답게" 보이겠지만, 원본 블록과의 중복 렌더링 방지 로직이 필요해 복잡도가 크게 늘어나는 데 비해 얻는 이득(시각적 테두리 하나)이 작다고 판단.

## 2026-07-28 9단계 — 브라우저 검증 중 발견: `ClassAggregate`의 IntersectionObserver가 자동화 탭에서 안 켜짐 (앱 버그 아님)

**결정**: `ClassAggregate.tsx`는 코드 수정 없이 그대로 둔다. 검증은 (1) `api.getAggregate`를 직접 호출해 두 학생의 응답이 정확히 집계되는지 확인하고, (2) React 파이버의 `visible` state를 직접 `dispatch(true)`로 강제해 폴링·렌더링 로직 자체가 올바르게 동작하는지 확인하는 방식으로 우회했다.

**이유**: claude-in-chrome으로 연 플레이어 탭에서 `document.hidden`이 계속 `true`(`visibilityState:'hidden'`)였다 — 이 자동화 세션의 탭들이 실제 Chrome 창에서 OS 차원의 "포그라운드"로 전환되지 않기 때문으로 보인다(`docs/PROGRESS.md`의 기존 미해결 이슈, `document.hasFocus()` 항상 false와 같은 근본 원인으로 추정). Chrome은 `visibilityState:'hidden'`인 탭에서 `IntersectionObserver` 콜백 자체를 지연/중단시키므로, `visible` state가 `false`에 머물러 폴링이 시작되지 않았다. 실제로 파이버 훅을 통해 `setVisible(true)`를 강제 호출하자 `api.getAggregate`가 정확한 카운트(2명, 9.8/3.7 각각 1)를 반환하고 차트가 정상 렌더링됨을 확인했다 — 즉 컴포넌트 로직 자체는 올바르고, 문제는 순수히 이 자동화 환경이 탭을 진짜 포그라운드로 만들지 못한다는 한계다.

**버린 대안**:
- *IntersectionObserver를 다른 가시성 판정 방식(예: `document.visibilityState` 무시하고 항상 폴링)으로 교체* — 실제 사용자 환경(교사·학생이 실제로 화면을 보는 상태)에서는 IntersectionObserver가 정상 동작하므로, 자동화 환경의 한계 때문에 프로덕션 코드를 바꾸는 것은 본말전도라 기각.
- *`computer` 툴로 탭을 실제로 클릭해 포그라운드 전환 시도* — 클릭 직후에도 `document.hidden`이 `true`로 남아 있어 이 방법으로는 해결되지 않음을 확인, 더 이상 시도하지 않음.

## 2026-07-28 11단계 — isTest는 editToken으로 검증하고, getProgress의 studentKey 조회는 그대로 둔다

**결정**: `saveProgress`/`submitResponse`가 `record.isTest`를 그대로 믿지 않고, 함께 온 `editToken`이 그 수업의 진짜 편집 권한과 일치할 때만 인정한다(`mock.ts`/`Code.gs` 양쪽에 `resolveIsTest` 추가). 반면 `getProgress(code, studentKey)`가 `studentKey`만으로 아무나 진행상황을 조회할 수 있는 부분은 **의도적으로 손대지 않는다.**

**이유**: 테스트 모드가 실제 기능이 되는 이 단계에서, `isTest:true`를 아무나 위조할 수 있다면 학생이 자기 응답을 통계·엑셀에서 슬쩍 빼거나(작은 문제) 반대로 테스트 응답인 척 진짜 학생 데이터에 섞일 여지가 생긴다 — editToken은 이미 6단계부터 있는 검증된 소유권 증명 수단이라 그대로 재사용하면 되는, 범위가 작고 명확한 수정이었다. 반면 `getProgress`의 studentKey 조회는 근본적으로 다른 문제다 — 이 프로젝트는 "로그인 없이 수업 코드 + 이름/학번만으로 식별"하기로 이미 확정된 설계(`docs/PLAN.md` 확정 사항)이고, studentKey 자체가 곧 학생의 유일한 "신분증"이다. 이걸 알아야만 접근 가능하게 만드는 것 이상의 보호(예: 세션 토큰 발급)를 추가하면 사실상 로그인 시스템을 새로 만드는 것과 같아, 애초에 "로그인 없음"을 선택한 이유(교사가 설정 없이 바로 쓴다, 학생이 회원가입 없이 참여한다)와 정면으로 부딪힌다.

**버린 대안**:
- *getProgress도 editToken 비슷한 별도 학생 토큰으로 보호* — 로그인 없는 설계를 사실상 뒤집는 것이라 기각. 필요해지면 완전히 새로운 ADR로 다뤄야 할 결정이지, 11단계 안에서 곁가지로 처리할 사안이 아니다.
- *isTest 위조를 막지 않고 그냥 둠* — 테스트 모드가 실제 기능이 되는 시점에 방치하면 나중에 "왜 이때 안 고쳤지"로 되돌아올 게 뻔해서(9~10단계에서부터 TODO로 문서화해뒀던 항목), 비용이 작은 지금 바로 처리.

## 2026-07-28 11단계 — GitHub Pages 배포는 리포 Variables로 mock/live 전환하는 단일 워크플로

**결정**: `.github/workflows/deploy.yml` 하나로 `main` 푸시마다 테스트→빌드→배포한다. `VITE_API_MODE`/`VITE_APPS_SCRIPT_URL`을 워크플로 파일에 하드코딩하지 않고 리포 Settings → Secrets and variables → Actions → **Variables**(Secrets 아님)에서 읽어온다. 지금은 비어 있어 mock 모드로 배포되고, 나중에 Apps Script를 배포한 뒤 이 두 값만 채우면 코드/워크플로 수정 없이 다음 배포부터 live로 전환된다.

**이유**: Apps Script 배포 URL은 editToken 없이는 아무 것도 못 하는 공개 엔드포인트라 원래도 비밀이 아니었다(`docs/OPERATIONS.md`에 이미 그렇게 적혀 있음) — 그래서 암호화된 Secrets가 아니라 평문 Variables로 충분하다. 워크플로 파일을 건드리지 않고 값만 갈아끼우게 만들면, "Apps Script 배포는 사용자만 할 수 있다"는 제약과 "그 다음 전환은 최대한 마찰 없이"를 동시에 만족시킨다.

**버린 대안**: `.env.local`을 커밋해 워크플로가 그대로 읽게 함 — `.env.local`은 애초에 `.gitignore`에 있고(0단계 결정), 이유가 있어 뺀 파일을 다시 커밋하는 건 앞뒤가 안 맞아 기각.

## 2026-07-28 12단계 — 레지스트리의 `icon` 필드를 아예 삭제하고, 슬래시 메뉴는 텍스트 전용으로

**결정**: `blocks/types.ts`(`BlockDefinition`)와 `blocks/questions/types.ts`(`QuestionDefinition`)에서 `icon: string` 필드를 완전히 제거했다(21개 등록 전체에서 `icon: '...'` 삭제). `SlashMenu.tsx`는 아이콘 없이 텍스트 라벨만 보여준다. 버튼·배너·안내문에 흩어져 있던 이모지(🔒📊📚⚠️🎉📱🖥️🔗 등)도 전부 텍스트로 바꿨다. 다만 `✕`(삭제)·`✓`/`✗`(정오답)·`⠿`(드래그 핸들)·`↶`/`↷`(실행취소·다시실행)·`⤷`/`⧉`(`SlideList.tsx`의 좁은 hover 아이콘 행)는 그대로 뒀다.

**이유**: 사용자가 "아이콘 사용 배제, 필요한 일부 경우에만 단색 아이콘"이라고 명시적으로 지시했다(2026-07-28, `memory/interactive-class-icon-policy` 참고). 슬래시 메뉴 20개 항목에 아이콘이 있으면 스캔하기 편해 보일 수 있지만, 20개를 전부 의미 있게 구분되는 단색 SVG로 새로 그리는 건 비용이 크고, 텍스트 라벨만으로도 충분히 스캔 가능하다고 판단해 기본값은 "아이콘 없음"으로 잡았다. 반면 남겨둔 기호들은 두 가지 조건을 동시에 만족한다 — (1) 색이 있는 그림문자(emoji-presentation)가 아니라 `currentColor`를 상속하는 순수 텍스트 글리프이고, (2) `SlideList.tsx`의 hover 아이콘 행처럼 텍스트 라벨이 물리적으로 들어갈 공간이 없는 진짜 "필요한 경우"다. 이게 사용자가 말한 예외 조건에 정확히 부합한다고 판단했다.

**버린 대안**:
- *CSS `grayscale` 필터로 기존 이모지를 무채색으로만 바꿔서 유지* — 필드 삭제 없이 빠르게 처리할 수 있는 방법이었지만, 사용자의 "배제"라는 표현이 단순히 "색을 빼라"보다 "웬만하면 아이콘 자체를 없애라"에 가깝다고 판단해 채택하지 않았다. 또한 필터 방식은 20개 이모지를 하나하나 손으로 고른 것보다 코드가 더 지저분해진다(어디에 필터를 적용했는지 흩어져 관리 포인트가 늘어남).
- *20개 슬래시 메뉴 항목마다 새 단색 SVG 아이콘 제작* — 시각적으로는 가장 좋겠지만, 아이콘 디자인 품질을 이 세션에서 담보하기 어렵고(브라우저로 픽셀 단위 확인 없이는 좋은 아이콘인지 판단 불가), "심플한 디자인"이라는 원래 목표에는 텍스트 전용이 오히려 더 잘 맞는다고 판단.

## 2026-07-28 10단계 — `xlsx`(SheetJS) 패키지의 공개 취약점을 감수하고 그대로 채택

**결정**: `.xlsx` 내보내기를 위해 `xlsx`(SheetJS) 패키지를 그대로 설치해 쓴다. `npm audit`은 이 패키지에 대해 high severity 취약점 2건(`GHSA-4r6h-8v6p-xvw6` 프로토타입 오염, `GHSA-5pgg-2g8v-p4x9` ReDoS)을 표시하고 "No fix available"라고 안내하지만, 대체 패키지로 바꾸지 않았다.

**이유**: 두 취약점 모두 `XLSX.read`/`XLSX.readFile`로 **신뢰할 수 없는 외부 xlsx/csv 파일을 파싱**할 때 공격 표면이 생기는 구조다(악의적으로 조작된 셀 서식·수식 문자열이 파서를 공격). 이 프로젝트에서 `xlsx`는 `lib/xlsx.ts`에서 `XLSX.utils.aoa_to_sheet`로 **교사 자신의 응답 데이터를 새 워크북으로 만들어 쓰기만** 하고(`XLSX.writeFile`), 외부에서 받은 xlsx 파일을 읽는 기능은 프로젝트 어디에도 없다(수업 가져오기는 `.json`만 받는다, `lib/portable.ts`). 즉 취약점이 걸려 있는 코드 경로 자체를 아예 실행하지 않는다 — 2026-07-28 「react-router-dom 취약점 경고」 결정과 같은 논리다.

**버린 대안**:
- *`exceljs` 등 다른 라이브러리로 교체* — 대안 라이브러리도 완전히 취약점이 없다는 보장이 없고, 어차피 쓰기 전용이라 실제 위험이 없는 상황에서 마이그레이션 비용만 발생해 기각.
- *`npm audit fix --force`로 강제 조치* — 애초에 "no fix available"라 의미 없음.

**주의**: 앞으로 이 프로젝트에 xlsx/csv **업로드(가져오기)** 기능을 추가하는 결정을 내릴 경우, `XLSX.read`/`readFile`을 쓰게 되면 이 취약점이 실제로 적용되므로 그때는 반드시 이 결정을 재검토할 것.

## 2026-07-28 리포지토리를 Public으로 전환, 수업 자동 만료(180일)는 이번 범위에서 제외

**결정**: GitHub 리포 `chan7881/class`를 Private → Public으로 전환했다(사용자가 직접 결정하고, GitHub의 sudo-mode 비밀번호 확인 단계도 사용자가 직접 입력해 완료함 — 비밀번호 입력은 어떤 경우에도 대신 하지 않는다는 원칙에 따라 여기서 멈추고 사용자에게 넘겼다). `docs/PLAN.md`에 명시된 "수업별 자동 만료(기본 180일) 옵션"은 이번 세션 범위에서 구현하지 않고 TODO로 남긴다. 대신 "사진 업로드 문항이 있으면 교사에게 경고 배너 표시"는 구현했다(`src/components/MediaPrivacyNotice.tsx`, `Photo.tsx`/`Drawing.tsx` 에디터에 부착 — 그리기도 학생이 그린 PNG를 Drive에 올리므로 사진과 동일하게 취급).

**이유**: GitHub Pages 무료 배포가 개인 계정에서는 Public 리포에만 되므로 11단계(실제 배포) 전에 리포 공개 여부를 정해야 했는데, 코드 자체엔 비밀값이 없어(Apps Script URL은 공개돼도 무방, editToken은 애초에 커밋 안 함) Public 전환에 실질적 위험이 없다고 판단해 사용자가 바로 결정했다. 자동 만료는 실제로 구현하려면 Apps Script에 시간 기반 트리거(만료된 수업의 Drive 파일·응답 시트를 찾아 삭제하는 배치 작업)를 새로 만들어야 해서 범위가 크고, 지금도 교사가 결과 페이지에서 수동으로 "수업 데이터 완전 삭제"를 누를 수 있어(10단계에서 구현) 당장 없어도 개인정보 보호의 최소 안전장치는 있는 상태다. 사진 경고 배너는 구현 비용이 작고(문항 에디터 UI 한 줄) 개인정보 고지 취지에 직접 부합해 바로 처리했다.

**버린 대안**:
- *자동 만료를 지금 구현* — Apps Script 트리거·배치 삭제 로직·교사에게 만료 기간 설정 UI까지 필요해 10~11단계 범위를 벗어난다고 판단해 보류.
- *sudo-mode 비밀번호 입력을 자동화로 우회* — 세션 쿠키 재사용이나 다른 방법을 시도하지 않고 즉시 사용자에게 안내하고 멈췄다. 계정 인증 정보 입력은 어떤 명분으로도 대신하지 않는다는 원칙을 그대로 지켰다.

## 2026-07-28 Apps Script 실제 배포 + 실배포 검증 중 발견한 경쟁 조건 버그 수정

**결정**: 사용자가 script.google.com에서 프로젝트 생성·OAuth 동의까지 직접 마치고 배포 URL을 전달했다. 이후 `.env.local`을 live로 채워 실제 백엔드로 전체 플로우(수업 생성→발행→학생 제출→결과 확인)를 검증하다가, "제출 완료" 화면까지 봤는데 교사 결과 화면엔 "진행중"으로 남는 실제 버그를 발견했다. 원인은 `Player.tsx`의 디바운스 자동저장(`saveProgress`, submittedAt 없음)이 방금 끝난 `submitResponse`(submittedAt 있음)보다 늦게 서버에 도착해 덮어쓰는 경쟁 조건이었다. `mock.ts`/`Code.gs` 양쪽의 `saveProgress`에 "이미 submittedAt이 있는 응답은 그대로 두고 무시"하는 방어를 추가하고, `Player.tsx`에도 제출 시 예약된 자동저장 타이머를 명시적으로 취소하는 방어를 더했다. 수정한 `Code.gs`는 다시 script.google.com에 붙여넣어 저장하고, **기존 배포를 새 버전으로 편집**해 재배포했다(URL 안 바뀜, 사용자가 직접 "배포 관리 → 새 버전" 절차 수행) — 재배포 후 같은 경쟁 조건을 다시 재현해 고쳐졌음을 확인했다.

**이유**: mock 백엔드(`localStorage`)는 모든 호출이 사실상 동기적으로 즉시 끝나기 때문에, "쓰기 두 개가 순서를 바꿔 도착하는" 종류의 버그가 애초에 발생할 틈이 없다. Apps Script는 호출마다 실제로 수 초가 걸리는 원격 서비스라, 실제 배포로 전체 플로우를 검증하기 전까지는 이런 경쟁 조건이 있는지조차 알 수 없었다 — `docs/PLAN.md`가 "실제 백엔드로 7~14번을 반복 검증하라"고 못박아둔 이유가 정확히 이거였다. 서버 쪽 방어(이미 제출된 응답은 자동저장이 못 건드림)를 `enforceLocks`와 같은 철학으로 추가한 이유는, 클라이언트 타이머 취소만으로는 이미 네트워크로 나간 요청까지는 막을 수 없어서 최종 방어선은 서버에 있어야 하기 때문이다.

**버린 대안**:
- *클라이언트에서 타이머만 취소하고 서버 방어는 생략* — "제출 버튼 클릭 시점에 아직 안 나간 자동저장"은 막지만, 이미 네트워크로 전송된 자동저장 요청이 응답 대기 중인 경우는 막지 못한다. 실제 사용자 환경에서 재현 가능성이 남아 근본적 해결이 아니라고 판단해 서버 방어를 필수로 추가.
- *saveProgress 자체를 없애고 submitResponse 시점에만 저장* — "새로고침해도 이어서 진행"이라는 PLAN.md의 핵심 요구사항(자동저장)을 통째로 포기하는 것이라 기각.

**Apps Script 편집기 자동화 관련 메모(다음에 비슷한 일을 할 때 참고)**: script.google.com의 코드 에디터는 Monaco 기반이라 `window.monaco.editor.getModels()`로 모델을 찾아 `.setValue(...)`로 내용을 통째로 바꿀 수 있다. 다만 큰 파일(1000줄 이상)을 automation 스크립트에 문자열로 직접 박아 넣으면 크기 문제가 생기므로, 로컬에서 임시 CORS 허용 정적 서버(Node `http` 모듈 몇 줄)를 띄우고 `fetch`로 가져와 `setValue`에 흘려보내는 방식이 안전하고 빨랐다(Vite dev 서버 자체는 기본적으로 CORS 헤더를 안 보내 다른 오리진에서 바로 fetch가 안 됨). 저장 버튼("Drive에 프로젝트 저장")은 `aria-label`로 안정적으로 찾아지지만, **"배포" 드롭다운 버튼은 DOM에 숨겨진 복제 노드가 있어 텍스트 매칭이 자꾸 엉뚱한 걸 집었다** — 결국 `getBoundingClientRect()`로 실제 버튼 크기(88×36px)를 가진 노드를 걸러서 찾아야 했다. 그래도 최종적으로 신뢰도가 낮아, **"배포 관리 → 새 버전 → 배포" 자체는 자동화하지 않고 사용자에게 넘겼다** — 잘못 누르면 새 배포가 생겨 URL이 바뀔 위험이 있는 되돌리기 어려운 조작이라, 애매하면 사용자에게 넘기는 원칙을 그대로 따랐다.

## 2026-07-29 GitHub Pages를 live 모드로 전환 — 리포 Variables 등록 + 수동 재배포

**결정**: 사용자가 "니가해"(대행 지시)라고 명확히 말해, GitHub 리포 Settings → Secrets and variables → Actions → Variables에 `VITE_API_MODE=live`/`VITE_APPS_SCRIPT_URL`을 claude-in-chrome으로 직접 등록했다. 등록 후 다음 `main` 푸시를 기다리지 않고 `workflow_dispatch`(Actions 탭의 "Run workflow")로 배포를 즉시 수동 트리거해 반영시켰다. 배포 완료 후 실제 사이트에서 "새 수업 만들기"를 눌러 네트워크 요청이 `script.google.com`으로 나가 200을 받는 것까지 확인해 live 전환을 최종 검증했다.

**이유**: 리포 Variables 등록은 OAuth·비밀번호 입력 같은 계정 인증 절차가 전혀 없는 평범한 GitHub 설정 폼 제출이라 "사용자만 할 수 있는 행동" 범주에 들지 않는다(11단계 GitHub Actions Pages Source 전환 때와 같은 판단). 워크플로 자체는 `main` 푸시로만 트리거되게 짜여 있어(`docs/DECISIONS.md` 11단계 항목), Variables만 바꾼 것으로는 다음 실제 코드 푸시 전까지 반영되지 않는다 — `workflow_dispatch` 트리거가 이미 워크플로 파일에 있었으므로, 불필요한 빈 커밋을 만드는 대신 그걸 그대로 활용해 즉시 검증까지 마쳤다.

**버린 대안**:
- *더미 커밋을 만들어 `main`에 푸시해 배포 트리거* — `workflow_dispatch`가 이미 존재해 코드 변경 없이도 같은 효과를 낼 수 있는데 굳이 의미 없는 커밋을 히스토리에 남길 이유가 없어 기각.
- *다음 사용자의 자연스러운 푸시를 기다림* — "니가해"로 명시적으로 위임받은 작업을 반쯤 걸쳐두는 셈이라, 즉시 끝까지 검증하는 쪽을 택함.

**자동화 관련 메모(실패 원인·재현 방법)**: 첫 번째 `VITE_API_MODE` 제출 시도는 Name/Value 필드 값을 DOM에 정확히 채워넣고(네이티브 setter + `input`/`change` 이벤트 디스패치) "Add variable" 버튼을 `.click()`했는데도 리스트 페이지에 반영되지 않았다(원인 불명 — 아마 GitHub의 폼 제출이 클릭 시점의 `pointerdown`/`mousedown` 시퀀스나 CSRF 토큰 타이밍에 더 엄격하게 의존하는 것으로 추정, 이 세션에서 근본 원인까지는 확인하지 못함). **재시도 시 제출이 실제로 페이지 이동(같은 탭의 CDP 실행 컨텍스트 파괴, `"Execution context was destroyed"` 에러)을 일으키는지로 성공 여부를 판별**했다 — 폼 제출 버튼이 `type="submit"`이고 성공 시 리스트 페이지로 리다이렉트되므로, 클릭 직후 같은 세션에서 JS를 실행해 "컨텍스트 파괴" 에러가 나면 실제 네비게이션(=제출 성공)이 일어난 것으로 간주할 수 있다. 두 번째 시도부터는 이 방식으로 매번 성공을 확인했다.

## 2026-07-29 사용자 신고 버그·기능 요청 일괄 처리 (14건)

**결정**: 아래 항목을 이번 세션에서 함께 처리했다. 각 결정의 요지만 남기고, 코드 자체의 주석에 이유를 적어둔 곳은 중복 기록하지 않는다.

1. **QR/직접입장 링크** — 외부 API(구글 차트 등)에 의존하지 않고 클라이언트에서 직접 QR을 그리는 방식을 택함(`qrcode` 패키지, SVG 렌더). 이유: 외부 API는 학생 기기 네트워크 상태나 서비스 중단에 취약하고, 수업 코드라는 사소하지 않은 정보를 제3자 서버에 매번 노출하는 것도 불필요하다.
2. **전체 수업 관리 화면(관리자)** — 새 Apps Script 액션(`listLessons`)을 추가해 운영자 Drive의 모든 수업 메타데이터를 조회하도록 함. **비밀번호는 서버에 해시로 저장하지 않고, 배포 시점에 스크립트 속성(Script Properties)에 운영자가 직접 넣는 방식**을 택함 — 이 프로젝트는 애초에 "인증 없음, code/editToken으로 소유권 증명"이 확정 아키텍처(CLAUDE.md)라, 학생/교사용 로그인 체계를 새로 만들지 않고 운영자 한 명만 접근하는 화면이므로 별도 계정 시스템 없이 "관리자 비밀번호 하나"로 충분하다고 판단. mock 모드는 별도 처리 없이 로컬 스토리지의 전체 수업을 나열한다(로컬 개발 편의, 실제 운영 위험 없음).
3. **`drive.google.com/uc?export=view&id=...` → `lh3.googleusercontent.com/d/<id>`** — TODO 목록에 위험으로 남겨뒀던 항목이 실제로 터진 것으로 판단(사용자가 보고한 "사진이 파일명만 보이고 안 뜬다" 증상이 Google이 `uc?export=view`에 바이러스 검사 경고 HTML을 돌려주는 최근 동작과 정확히 일치). `Code.gs` 재배포가 필요하다 — 사용자가 직접 해야 하는 단계.
4. **`lib/xlsx.ts`의 "XLSX.read는 어디서도 쓰지 않는다" 결정에 대한 예외** — 차트 블록에 엑셀 업로드를 추가하며 `src/lib/spreadsheet.ts`에서 `XLSX.read`를 처음 사용했다. 기존 결정이 막으려던 위험은 "신뢰할 수 없는 파일이 다른 사용자에게 영향을 주는 것"인데, 여기서는 업로드하는 사람과 결과를 보는 사람이 같은 교사 자신이고, 파싱 결과는 즉시 숫자/문자열 원시값 배열로만 남아 원본 파일이나 워크북 객체를 어디에도 보관·재파싱하지 않는다 — 위험의 성격이 달라 예외를 허용. 대신 5MB 업로드 크기 제한을 뒀다(`MAX_SPREADSHEET_BYTES`).
5. **서답형 "필수 키워드 조합" 문법** — eval 없이 직접 만든 파서(`lib/keywordMatch.ts`, CLAUDE.md 규칙 2)로 "쉼표=AND, 괄호 안 쉼표=OR" 문법을 처리. 부분 일치는 `GradeResult`에 `partial?: boolean` 필드를 추가해 표현(정답 여부는 여전히 `correct:false`로 두고 UI만 노란색으로 구분) — 기존 `correct`/`points` 계약을 깨지 않으면서 점수 체계(결과 대시보드의 `sum(s.points)`)가 절반 점수를 자동으로 반영하게 하기 위함.
6. **데이터표 문항 채점 제거** — `DataTableQuestion.answerTargets`와 그 UI, `grade` 등록, `Code.gs`의 추세선 회귀 채점 코드(약 250줄, 오직 그 용도로만 쓰이던 수식 파서 포함)를 전부 삭제. 그리기·사진과 같은 "grade 미등록 = 교사 수기 확인" 패턴으로 통일.

**버린 대안**:
- *QR을 외부 이미지 생성 API로 그림* — 구현은 더 간단하지만 위 이유로 기각.
- *관리자 화면 인증을 별도 로그인/JWT로 구현* — 이 프로젝트의 "인증 없음" 아키텍처와 정면으로 충돌하고 요구사항 대비 과함. 단일 비밀번호 게이트로 충분하다고 판단.

**아이콘 없는 네이티브 대화상자(alert/confirm) 관련 메모**: 이 검증 도중 만든 테스트 수업(`BECGXB`)을 정리하려고 결과 페이지의 "수업 데이터 완전 삭제" 버튼을 클릭했는데, 이 버튼이 여는 `window.confirm()`이 렌더러를 블로킹해 이후 모든 CDP 명령(`Input.dispatchMouseEvent`, `Runtime.evaluate`)이 타임아웃났다. 기존에 문서화된 "얼럿/다이얼로그를 트리거하는 요소는 클릭하지 말 것"이라는 일반 원칙이 실제로 이 프로젝트에서 처음 발동한 사례다 — 삭제 버튼은 사용자에게 넘기고 다른 탭으로 작업을 이어갔다(`docs/PROGRESS.md` 미해결 이슈에 재발 방지용으로 기록).

## 2026-07-29 Apps Script 코드 배포를 어시스턴트-사용자 분담으로 고정 — claude-in-chrome 자동화 방법 확립

**결정**: `Code.gs` 재배포 절차 5단계 중 1~3단계(프로젝트 열기, 코드 교체, 저장)는 어시스턴트가 claude-in-chrome으로 매번 대신 수행하고, 4~5단계("배포" 클릭, `ADMIN_PASSWORD` 등록)만 사용자가 한다 — 사용자가 명시적으로 확정한 분담(`CLAUDE.md`에 반영). 정확한 자동화 방법은 여러 번 실패 끝에 아래로 확립했다.

**이유·경위(실패 3번, 성공 1번)**:
1. **1차 실패**: `model.setValue(newText)`로 Monaco 모델을 교체한 뒤, `read_page`로 얻은 파일 탭 버튼 ref를 클릭해 "Code.gs 탭으로 전환"을 시도했는데, 그 ref가 실제로는 다른 엘리먼트(사이드바의 "별표표시된 프로젝트" 메뉴)를 가리켜 `/home/starred`로 네비게이션돼버렸다. Apps Script 편집기 SPA는 라우트가 바뀌면 에디터 컴포넌트가 언마운트되는 것으로 보이고, 저장 안 된 Monaco 모델의 변경은 그대로 유실됐다(다시 프로젝트를 열어보니 원래 내용으로 돌아가 있었음).
2. **2차 실패**: 다시 `model.setValue()`로 교체 후, 이번엔 `read_page`로 매번 새로 얻은 ref로 "Drive에 프로젝트 저장" 버튼을 두 번 클릭했다. 클릭 자체는 성공했다고 보고됐고 "저장되지 않은 변경사항" 문구도 보였지만, **페이지를 새로고침해서 재확인하니 여전히 옛 내용(1113줄)** — 저장이 전혀 반영되지 않았다. `computer` 툴의 버튼 클릭이 이 특정 저장 버튼에 대해 신뢰할 수 없다는 뜻으로 판단(이 프로젝트에서 `computer` 클릭이 불안정하다는 기존 기록과 일치).
3. **원인 추정**: `model.setValue()`는 Monaco 모델 자체는 바꾸지만, Apps Script 호스트 앱(Angular)이 "저장할 변경사항이 있다"고 인식하는 경로가 `setValue`가 발생시키는 이벤트와 다를 수 있고, 저장 버튼 클릭도 좌표/타이밍이 앱의 클릭 핸들러와 안 맞았을 가능성이 있다.
4. **성공한 방법**: (a) 파일 목록에서 `Code.gs` 탭을 먼저 클릭해 실제로 포커스된 활성 에디터로 만들고 `monaco.editor.getEditors()[0].hasTextFocus()`로 확인, (b) `model.setValue()` 대신 **`editor.executeEdits('replace', [{range: model.getFullModelRange(), text: 새내용}])`** 로 교체(진짜 사용자 편집과 같은 이벤트 경로), (c) 저장 버튼을 클릭하지 않고 **`computer` 툴의 `key` 액션으로 `ctrl+s`** 를 눌러 저장(에디터에 포커스가 있어야 함) — 화면에 "저장 중..." 텍스트가 뜨는 걸 확인했고, **페이지를 새로고침해 모델 값이 로컬 파일과 문자 그대로(`===`) 일치**함을 확인해 실제 저장을 검증했다.
5. **964줄(33KB) 텍스트를 automation 스크립트 문자열에 직접 박아 넣지 않고**, 로컬 임시 정적 HTTP 서버(Node `http`, CORS 헤더 포함)를 스크래치패드에 띄워 페이지 안에서 `fetch`로 가져오는 기존에 확립된 방법(2026-07-28 "Apps Script 편집기 자동화 관련 메모" 항목)을 그대로 재사용했다. 작업 후 서버는 종료.

**버린 대안**:
- *`model.setValue()` + 저장 버튼 클릭* — 위에서 두 번 실패해 기각.
- *"배포" 버튼까지 어시스턴트가 클릭* — 되돌리기 어려운 배포 조작이라 사용자 본인 확인이 필요한 행동으로 분류, 사용자도 "내가 배포 누르고 알려줄게"라고 명시적으로 그렇게 요청함 — 항상 사용자에게 남긴다.
- *저장 성공 여부를 "저장 중..."/"저장되지 않은 변경사항" 같은 화면 텍스트로만 판단* — 2차 실패에서 이 텍스트들이 실제 저장 여부와 안 맞는 것을 확인했다(정적으로 항상 DOM에 있는 라벨일 가능성). **페이지 새로고침 후 내용 직접 비교만 신뢰할 것.**

## 2026-07-29 수식 문항에 물리 키보드 입력 허용 + 영문자 버튼판 추가, 화학식 화살표 3종 추가

**결정**: `math/MathField.tsx`가 그동안 완전히 막고 있던 물리 키보드 타이핑·붙여넣기(`keydown`/`paste`/`cut` 캡처 차단, 7단계에서 도입)를 제거해 물리 키보드 입력도 버튼과 함께 허용한다. 대신 `lib/mathKeyboards.ts`에 `letters` 레이어(A-Z, a-z 52개 버튼)를 새로 추가해 **키보드가 없어도 버튼만으로 영어 알파벳까지 전부 입력 가능**하게 유지한다. 새 문항 기본값(`Math.tsx`의 `createDefault`)에 `letters`를 `basic`과 함께 기본 포함. 화학식 문항(`Chem.tsx`의 `CHEM_BUTTONS`)과 수식의 화학 레이어(`mathKeyboards.ts`의 `chem`)에 `↑`(기체 발생)·`↓`(침전)·`↔`(양방향) 버튼 3종을 추가.

**이유**: 사용자가 "수식 문제에서 영어가 입력안됨"을 실사용 중 신고했다 — `basic` 레이어에 `x`/`y`만 있고 다른 영문자(`F`, `v`, `a`, `m` 등 물리 변수명)를 넣을 방법이 아예 없었던 게 원인. 사용자가 "전반적으로 키보드 입력도 같이 되는지 확인"이라고 명시적으로 요청해, CLAUDE.md 규칙 7("학생 입력은 버튼 기반")을 완화하되 "버튼만으로도 항상 끝까지 가능해야 한다"는 핵심은 유지하는 쪽으로 해석했다 — 키보드는 추가 수단이지 버튼의 대체가 아니다. 화학식 화살표는 기체 발생(↑)·침전(↓) 표기가 실제 화학반응식에 자주 쓰이는데 기존엔 `→`/`⇌`만 있어서 표현 못 하던 반응식들이 있었다.

**참고**: 화살표 3종은 `chemNormalize.ts`의 정규화 로직 변경이 필요 없었다 — 기존 첨자·화살표 치환 규칙과 겹치지 않는 새 리터럴 문자라 그대로 비교돼도 안전하다(→를 `⇌`로 오인식하던 순서 버그와 달리 겹치는 패턴이 없음). `Code.gs`의 `gradeChem`도 동일 로직이라 별도 이식 불필요.

**버린 대안**:
- *영문자도 버튼만 추가하고 키보드는 계속 막기* — 사용자가 명시적으로 "키보드 입력도 되는지 확인"을 요청해 기각. 버튼만으로는 대문자/소문자 각 26개씩 눌러야 해서 특히 여러 글자짜리 변수명(예: `Fnet`)을 쓸 때 번거로움이 남는데, 키보드를 열어주면 그 불편이 해소된다.
- *`letters` 레이어를 만들지 않고 키보드 허용만으로 끝내기* — CLAUDE.md 규칙 7의 "버튼만으로도 항상 가능해야 한다" 원칙이 깨진다(키보드 없는 기기·키보드를 쓰기 싫은 학생을 배제). 기각하고 버튼판을 반드시 같이 추가.

## 2026-07-29 수식 버튼의 "빈 중괄호" 구조가 안 채워지던 버그 — MathLive `#?` 플레이스홀더 토큰 도입

**결정**: `mathKeyboards.ts`에서 분수·근호·절댓값·로그·지수·위첨자·아래첨자처럼 학생이 채워야 할 자리가 있는 버튼의 LaTeX 템플릿을 빈 `{}`에서 `#?`로 바꿨다(`\frac{}{}` → `\frac{#?}{#?}` 등). `mathNormalize.ts`(+`Code.gs` 이식)에 간격 전용 LaTeX 명령(`\,` 등)과 빈 중괄호 그룹을 채점 비교에서 무시하는 규칙을 추가.

**이유**: 사용자가 "분수 식이라던지 안 먹히는 문자들이 생김"을 신고했다. 원인 조사: MathLive의 `insert()`는 기본적으로 삽입 직후 첫 번째 "플레이스홀더"를 자동 선택해(`InsertOptions.selectionMode`, 기본값 `'placeholder'`) 바로 이어서 입력할 수 있게 하는데, 빈 `{}` 그룹은 이 메커니즘이 인식하는 진짜 플레이스홀더가 아니다(타입 선언만으론 이걸 확신할 수 없어서, 로컬에 설치된 `node_modules/mathlive/mathlive.js`(비압축) 소스를 직접 grep해 MathLive 자신의 기본 키보드 정의 — `"frac": "\frac{#?}{#?}"`, `"sqrt": "\sqrt{#?}"` 등 — 을 확인하고서야 `#?`가 정답이라고 확정했다). 버튼을 클릭해 `\frac{}{}`를 넣어도 커서가 그 구조 밖에 남아, 이어지는 버튼 입력이 분자·분모 밖으로 새 나가는 게 "안 먹힘"의 정체였다.

**검증**: 브라우저로 "a/b" 버튼 클릭 → 필드 값이 `\frac{\placeholder{}}{\placeholder{}}`(MathLive가 `#?`를 이 atom으로 치환)로 바뀌는 것 확인 → 이어서 "1", "2" 버튼 클릭 → `\frac{12}{\placeholder{}}`로 정확히 분자 자리에 들어가는 것까지 확인. 8단계 이후 이 프로젝트의 수식 버튼 브라우저 검증이 단순 사칙연산(`7+8`)만 했지 분수·근호류의 구조형 버튼은 한 번도 실클릭 검증을 안 했었다는 게 근본 원인이 이렇게 오래 남아있던 이유다.

**버린 대안**:
- *문서(타입 선언 주석)만 보고 `#@`를 쓰기* — 처음엔 `#@`(선택 영역 대체용 토큰)로 추측했으나, 소스에서 `#@`는 이미 선택된 내용을 감싸는 postfix 연산(예: 어깨 제곱 `#@^2`)에만 쓰이고, 우리처럼 선택 없이 클릭하는 경우엔 `#?`가 맞다는 걸 소스 대조로 확인하고 정정. 추측만으로 결정하지 않고 실제 라이브러리 소스를 확인하는 게 이번에 결정적이었다.

## 2026-07-29 관리자 화면 "수정" 버튼 — editToken을 재발급해서 연다(기존 링크는 무효화됨)

**결정**: 관리자 화면(`/#/admin`)에 "수정" 버튼을 추가했는데, 서버가 `editToken`을 해시로만 저장하고 평문을 절대 보관하지 않는다는 기존 원칙(CLAUDE.md 규칙 6) 때문에 관리자가 임의 수업의 평문 `editToken`을 조회할 방법이 없다. 그래서 새 백엔드 액션 `adminResetEditToken`(운영자 비밀번호 인증)을 추가해 **그 수업의 editToken을 새로 발급하고 `_index`의 해시를 덮어써** 관리자가 새 토큰으로 즉시 편집기를 열 수 있게 했다. `window.confirm`으로 "기존 편집 링크가 무효화된다"는 경고 후 진행한다.

**이유**: "다운로드 버튼 옆에 수정 버튼 추가"라는 요청을 만족하려면 관리자가 실제로 편집기에 들어갈 방법이 있어야 하는데, 평문 토큰을 저장하지 않는 보안 설계상 "몰래 훔쳐보기"는 애초에 불가능하게 만들어져 있다. 재발급은 이 제약 안에서 유일하게 실현 가능한 방법이었고, 삭제(완전 파괴)보다는 훨씬 약한 권한이라 기존에 관리자가 이미 가진 "수업 완전 삭제" 권한보다 위험도가 낮다고 판단해 사용자 확인 없이 구현하고, 대신 이 트레이드오프 자체를 이 문서와 세션 종료 보고에 명시해 사용자가 원치 않으면 되돌릴 수 있게 했다.

**트레이드오프(사용자 확인 완료, 2026-07-29)**: 관리자가 "수정"을 누르면 그 수업을 담당하던 교사가 저장해둔 기존 편집 링크(복구 링크·북마크 등)는 그 순간부터 동작하지 않는다. 사용자에게 "editToken을 평문으로 저장해 기존 링크를 살리는 방법도 있지만 보안 원칙(규칙 6)을 후퇴시킨다"고 설명하고 대안을 물었고, **평문 저장 없이 현재의 재발급 방식을 그대로 유지하기로 확정**했다 — 보안 원칙이 관리자 편의보다 우선한다는 판단. 이 주제는 다시 묻지 않는다.

**버린 대안**:
- *편집 없이 다운로드(.json)만 제공* — 이미 있던 기능이라 "수정 버튼 추가" 요청을 만족하지 못해 기각.
- *평문 editToken을 서버에 별도 저장해뒀다가 그대로 돌려주기* — CLAUDE.md 규칙 6("교사 editToken은 코드 어디에도 남기지 않는다")과 정면충돌하는 아키텍처 후퇴라 기각.

## 2026-07-29 수식 기호 레이어(`symbols`) 추가 — 부등호·근사·집합기호 등

**결정**: `lib/mathKeyboards.ts`에 `symbols` 레이어를 새로 추가했다(≤ ≥ ≠ ≈ ≡ ≃ ≅ ≑ ∝ ∼ ± ∓ ∈ ∉ ⊂ ⊃ ∀ ∃ ∅ ∠ ⊥ ∥ ∑ ∫ ∂ lim → ⇒ ⇔ ∴ ∵ 등 33개). `MathKeyboardLayer` 유니온·`Math.tsx`의 `ALL_LAYERS`/`LAYER_LABELS`에 반영.

**이유**: 사용자가 "부등호나 ≃≑∓등등 기본 수학 기호가 부족하다"고 지적, "자주 사용되는 기호 위주로" 탑재해달라고 요청했다. 각 기호를 실제로 넣기 전에 `node_modules/mathlive/mathlive.js`(비압축 소스)를 grep해 `\leq`·`\Doteq`·`\simeq`·`\varnothing`·`\therefore` 등 사용하려는 매크로가 전부 MathLive에 실제로 정의돼 있는지 확인했다(위 `#?` 버그 때와 같은 이유 — 문서·추측이 아니라 소스 대조).

**버린 대안**:
- *기존 레이어(`basic`/`fraction`)에 욱여넣기* — 버튼 수가 너무 늘어나 한 화면에서 훑어보기 어려워지고, 레이어별 목적(사칙연산 vs 관계/집합 기호)이 섞여 기각. 별도 탭으로 분리.
- *구현하기 전 MathLive 지원 여부를 문서만 보고 판단* — 과거 `#?` 버그 사례에서 문서만 믿었다가 틀렸던 적이 있어(위 항목 참고), 이번엔 반드시 소스 대조 후 확정.
