# CLAUDE.md — 이 프로젝트에서 작업하기 전에 반드시 읽을 것

> **작업을 시작하기 전에 `docs/PROGRESS.md`를 먼저 읽어라.** 현재 어느 단계까지 됐고 다음에 뭘 해야 하는지가 거기 있다. 이 파일은 규칙과 구조만 설명한다.
>
> 교육 자료 공통 규칙(결론을 미리 말하지 않기, 화면·다크모드 원칙, UI 공유 점검표, 배포 확인)은
> **`../CLAUDE.md`**에 있다. 여기에는 **이 프로젝트에만 해당하는 것**만 적는다.

## 프로젝트가 무엇인가

과학 교사가 Notion처럼 블록을 쌓아 슬라이드형 학습지를 만들고, 학생이 모바일/PC에서 슬라이드를 넘기며 문제를 풀고(문제를 풀지 않으면 다음으로 못 넘어감), 교사가 그 답안을 엑셀로 수합하는 웹 도구. Rubin Observatory의 Investigations 형식과 Notion의 블록 편집 UX를 참고한다. 전체 계획은 `docs/PLAN.md`에 있다.

## 확정된 아키텍처 (바꾸려면 `docs/DECISIONS.md`에 이유를 남긴 뒤 바꿀 것)

- **프론트엔드**: React + Vite + TypeScript, GitHub Pages(HashRouter)로 정적 배포
- **백엔드**: Google Apps Script 웹앱 **1개를 중앙 배포**(운영자 계정 소유), 모든 교사가 공유. 서버 없음.
- **데이터**: 운영자 Google Drive에 수업 JSON·미디어, Google Sheets에 학생 응답
- **인증 없음**: 수업은 `code`(학생용)와 `editToken`(교사용, 서버에는 해시만 저장)으로 소유권을 증명한다
- 상세 데이터 모델·API·화면 설계는 `docs/PLAN.md` 참고

## 반드시 지킬 코드 규칙

1. **`src/blocks/registry.ts` 패턴을 지킨다.** 블록·문항 유형은 전부 `{ type, label, defaultData, Editor, Viewer, grade?, isAnswered?, toCell?, describeAnswer? }` 객체로 등록한다. 새 유형 추가 = 파일 하나 추가 + 레지스트리 등록. 유형별 `switch`를 여러 파일에 흩뿌리지 않는다. 문항이 12종이라 이 구조가 무너지면 유지보수가 불가능해진다.
2. **`eval`/`new Function()` 금지.** 데이터표 계산 열(`lib/formula.ts`)은 직접 만든 안전한 파서로 평가한다.
3. **정답은 서버가 지킨다.** `getLesson`(학생용 API)은 정답·해설 필드를 반드시 제거하고 내려준다. 클라이언트 `lib/grade.ts`는 목 모드·에디터 미리보기·교사 테스트 모드에서만 쓴다.
4. **채점·검증 로직은 순수 함수로 분리한다** (`lib/grade.ts`, `units.ts`, `sigfigs.ts`, `regression.ts`, `numbering.ts`, `navigate.ts`, `validate.ts`, `chemNormalize.ts`, `mathNormalize.ts`). UI에 채점 로직을 박아넣지 않는다. 전부 Vitest로 테스트한다.
   - **`apps-script/Code.gs`는 이 채점 로직을 GAS로 손수 옮겨 적은 별도 사본이다 — 자동으로 동기화되지 않는다.** 채점·정규화 규칙을 추가하거나 바꿀 때마다 반드시 체크: `[ ]` 해당 TS 채점기(`lib/grade.ts` 또는 문항별 채점기) 수정 `[ ]` `Code.gs`의 대응 `gradeXxx`/정규화 함수도 같이 수정 `[ ]` 커밋 전 diff로 두 곳 다 손댔는지 재확인. 과거 두 번(7·8단계) 이 동기화를 놓쳐 채점 결과가 TS와 GAS에서 서로 달랐던 사고가 있었다(docs/DECISIONS.md 참고).
5. **스키마는 마이그레이션 가능해야 한다.** `Lesson.version`을 바꾸는 변경은 `lib/migrate.ts`에 변환 함수를 추가해야 한다. 옛 수업이 안 열리는 상황을 만들지 않는다.
6. **비밀값을 절대 커밋하지 않는다.** Apps Script 배포 URL은 `.env.local`(gitignore됨)에, 교사 `editToken`은 코드 어디에도 남기지 않는다.
7. **학생 입력은 버튼만으로도 항상 끝까지 가능해야 한다** (사용자 지시). 수식·화학식 입력에서 학생이 LaTeX 문법이나 키보드 단축키를 몰라도 버튼판만으로 답을 완성할 수 있어야 한다 — 새 기호를 추가할 땐 반드시 버튼도 같이 만든다. 화학식 첨자는 자동 변환 금지 — 명시적 토글 버튼으로만. **물리 키보드 타이핑은 버튼의 보완 수단으로 함께 허용한다**(2026-07-29부터, `math/MathField.tsx`는 더 이상 키보드 입력을 막지 않는다 — 영어 알파벳처럼 버튼판만으로는 부족한 경우를 보완하기 위해 사용자가 명시적으로 요청, `docs/DECISIONS.md` 참고). "버튼만으로 가능해야 한다"는 원칙 자체는 안 바뀌었다 — 키보드가 막혀도 버튼만으로 같은 답을 만들 수 있어야 통과.
8. **모바일 우선** (공통 규칙 3번). 이 앱이 그 규칙을 지키는 방법: 터치 타깃은 `.tap-target` 클래스가 보장하므로 직접 크기를 주지 말고 이 클래스를 쓴다. 하단 고정 바에는 `env(safe-area-inset-bottom)`를 넣는다.
9. **아이콘은 `lucide-react` 단색만** (원칙은 공통 규칙 3번). 이 앱 고유:
   - 크기·굵기는 `src/components/Icon.tsx` 래퍼로 sm(16)/md(20)/lg(24) 세 단계만 쓴다. `stroke="currentColor"`가 기본이라 글자색을 따라가고, 그래서 다크모드 토큰이 뒤집히면 아이콘도 같이 뒤집힌다 — **색을 하드코딩하면 다크모드가 깨진다.**
   - 아이콘 전용 버튼의 `aria-label`은 `Button`의 `iconOnly` prop이 **타입으로 강제**한다.
   - ⚠️ **과거의 오해**: 이 규칙을 "아이콘 전면 금지"로 읽은 탓에 2026-08-01 이전까지 코드베이스에 `<svg>`가 하나도 없었고, 아이콘 자리를 전부 텍스트로 때워 에디터 헤더에 텍스트 버튼 10개가 늘어섰다. 원래 의도는 "난잡한 컬러 그림 금지"이지 아이콘 금지가 아니다.

10. **다크모드에서 안 보이는 색을 만들지 않는다.** 이 앱은 `index.css`의 **디자인 토큰 값을 통째로 뒤집는 방식**으로 다크모드를 구현한다 — 컴포넌트는 토큰만 쓰면 저절로 대응된다. 문제는 토큰을 **우회한 색**이고, 실제로 2026-08-06에 "글자가 아예 안 보인다"는 신고가 들어왔다(원인·조치는 `docs/DECISIONS.md` 2026-08-06 항목).

   **색을 추가·수정할 때 반드시 확인할 것 (체크리스트)**
   - `[ ]` **배경 토큰이 뒤집히면 글자 토큰도 뒤집히는 것을 쓴다.** `bg-neutral-800`·`bg-neutral-900` 위에는 `text-white`가 아니라 **`text-neutral-0`**을 쓴다. `text-white`는 다크모드에서도 안 뒤집히므로, 뒤집히는 배경 위에 얹으면 흰 배경에 흰 글자가 된다. (`text-white`가 맞는 곳은 `bg-accent-500`·`bg-danger`처럼 **안 뒤집히는** 배경뿐이다.)
   - `[ ]` **강조색 글자에는 `text-accent-ink`를 쓴다.** `text-accent-500/600/700`은 금지 — 그 토큰들은 버튼 배경으로도 쓰여서 뒤집을 수 없고, 글자로 쓰면 어두운 배경에서 대비가 3:1 아래로 떨어진다.
   - `[ ]` **하드코딩 색(`#rrggbb`, `style={{color}}`)을 새로 넣지 않는다.** 꼭 필요하면 토큰(`var(--color-…)`)을 쓰거나, 두 모드 모두에서 배경과 구분되는지 직접 계산해 확인하고 그 근거를 주석에 남긴다.
   - `[ ]` **교사가 고르는 색(리치텍스트 팔레트 등)은 값이 수업 JSON에 박혀서 저장된다** — 반드시 토큰으로 저장한다(`richtext/toolbarOptions.ts` 참고). 고정 색으로 저장하면 나중에 손댈 방법이 없다.
   - `[ ]` **캔버스·이미지처럼 색을 CSS로 못 바꾸는 것**(그리기 판, QR코드)은 **항상 밝은 배경**으로 고정한다(`bg-white` 예외). 그리기 답안 PNG는 배경이 투명이라 표시하는 쪽에서 흰 바탕을 깔아준다.
   - `[ ]` 새 색을 넣었으면 **두 모드에서 실제로 확인**한다 — 확인 방법은 아래.

   **실측 방법은 공통 규칙 3번**을 따르되, 이 앱에서는 OS 다크모드를 못 켜므로 `index.css`의 다크모드 블록과 **같은 값을 `document.documentElement.style.setProperty(...)`로 주입**해 재현한다. 빌드는 `npx vite build` 결과물로(개발 서버는 일부 유틸리티를 `var()`가 아닌 literal로 내보내 변수 치환이 안 먹는다).

   (`text-neutral-400`처럼 일부러 흐리게 둔 보조 텍스트는 두 모드에서 대칭이면 예외로 둔다.)

11. **서버를 왕복하는 버튼에는 반드시 `BusyOverlay`를 띄운다.** 백엔드가 Apps Script라 한 번 왕복에 1~3초가 흔하고, 그동안 아무 반응이 없으면 사용자는 버튼이 안 눌린 줄 알고 다시 누른다 — 제출·발행·삭제가 두 번 실행되는 사고로 이어진다. `src/components/BusyOverlay.tsx` 하나만 쓰고 새로 만들지 않는다(문구만 바꾼다). 오버레이가 뒤쪽을 덮어 중복 클릭도 같이 막힌다.
   - 여러 건을 순서대로 처리할 때는 문구에 **진행 개수**를 넣는다(예: `수업을 삭제하는 중입니다… (3/10)`). 어디까지 됐는지 안 보이면 멈춘 줄 안다.
   - 배경 폴링·자동저장처럼 **사용자가 누른 게 아닌** 작업에는 띄우지 않는다 — 화면이 제멋대로 잠기는 것처럼 느껴진다.

12. **한 UI를 여러 목적으로 공유할 때는 공통 규칙 4번의 점검표를 쓴다.** 이 앱에서 실제로 난 사고: 홈의 코드 입력 패널을 "수업 수정하기"와 "수업 현황 보기"가 공유하게 만들면서 첫 안내문과 버튼만 분기시키고 **두 번째 입력칸을 손대지 않아**, 현황 암호가 *편집 키 자리*에 저장돼 그 경로로는 아예 못 들어갔다(2026-08-08). 분기 판단은 `lib/editorAuth.ts`의 `saveLiveSecret`/`looksLikeEditToken`에 모아 두었고 `editorAuth.test.ts`가 고정한다 — **새로 만들지 말고 이걸 쓸 것.**

## 13. 마무리 전에 예외 처리를 점검한다 ★

작업을 끝내기 전에 **저장된 값이 기대한 모양이 아닐 때 어떻게 되는지**를 한 번 훑는다. 사용자 지시(2026-08-18).

- `[ ]` **목록 전체를 훑는 자리**(`isAnswered`·집계·정렬·표 렌더)에서 나는 예외는 **피해가 화면 전체**다. 한 건이 잘못돼도 나머지가 보여야 한다.
- `[ ]` `.trim()`·`.length`·`.map()`을 부르기 전에 **그 값이 정말 문자열/배열인지** 확인한다. `?? ''`로 막았다고 끝이 아니다 — 원소가 `null`일 수 있다.
- `[ ]` **`Array.every`/`some`/`forEach`는 배열의 구멍을 건너뛴다.** 개수를 세야 하는 검사는 `Array.from({length: n}, (_, i) => arr[i])`처럼 **인덱스로 직접** 돈다.
- `[ ]` **쓰는 쪽만 고치지 않는다.** 이미 저장된 옛 데이터가 남아 있으므로 **읽는 쪽 방어**가 근본이다.
- `[ ]` `try/catch`로 통째로 덮지 않는다 — 원인이 사라지고 같은 종류의 다음 버그를 조용히 삼킨다.
- `[ ]` 서버를 왕복하는 곳에는 **실패했을 때 화면이 어떻게 되는지**를 정한다. 명단을 지우지 말고 "갱신 멈춤"만 알리는 식(`LivePage`).

> 2026-08-18에 학생 한 명의 빈칸 답에 들어간 `null` 하나로 `null.trim()`이 터져 **현황판 전체가 흰 화면**이 됐다. 수업 중이었고 원인이 안 보여 오래 걸렸다. 회귀 테스트를 쓰다 `every`가 구멍을 건너뛴다는 **두 번째 버그**까지 드러났다 — 점검을 절차로 두지 않으면 이런 건 안 보인다.

## 14. 「같은 학생인가」는 `lib/identity.ts` 가 정한다 ★

응답 행을 가르는 기준은 **다듬은 학년·반·번호·이름**이다. 다듬는 규칙은 `src/lib/identity.ts`
하나뿐이고, `studentKey` 도 반드시 그걸 거쳐 만든다(`lib/studentKey.ts`).

- `[ ]` **다듬기 규칙을 고치면 `apps-script/Code.gs` 의 `normalizeIdentity`·`identitySignature`도
  같이 고친다** (규칙 4와 같은 이유 — 한쪽만 고치면 화면값과 행 열쇠가 어긋난다).
- `[ ]` 열쇠 계산을 바꾸면 **옛 행을 못 찾는다.** 서버의 식별정보 대조(`findRowIndexForRecord`)가
  그 대비책이니, 그걸 지우거나 우회하지 마라.
- `[ ]` **수업 도중 기기가 바뀌어도 이어서 풀려야 한다**(사용자 요구 2026-08-19). 새 식별칸을
  추가하거나 진입 흐름을 손대면 이 시나리오를 실제로 눌러 보고 끝내라.
- `[ ]` 시트에 새 행을 만드는 코드는 **반드시 `withLock` 안에서** 돌리고, `withLock` 의
  `SpreadsheetApp.flush()` 두 줄을 지우지 마라 — 그게 없으면 **락이 있어도 행이 두 개 생긴다.**

> 2026-08-19 마찰전기 수업에서 한 학생의 행이 **같은 열쇠로 두 개** 생겼다. flush 가 없어
> 락을 넘겨받은 실행이 앞 실행의 새 행을 못 봤다. 락 안에서 행을 다시 찾는 코드는 이미
> 있었지만 **flush 없이는 그 재조회가 무의미**하다. 원인·대안은 `docs/DECISIONS.md` 참고.
>
> 이미 생긴 중복은 `mergeDuplicateResponses`(정비용, `dryRun: true` 로 먼저 확인)로 합친다.

## 15. 응답 시트를 건드리는 코드의 함정 ★

- `[ ]` **`ensureQuestionColumns` 는 컬럼만 만들고 값은 나중에 쓴다.** 그래서 한 저장에서
  새 문항이 둘 이상 생기면 먼저 만든 컬럼이 「빈 컬럼」으로 보여 다음 문항에게 빼앗긴다
  (2026-08-20, 실제로 답이 사라졌다). `_allocatedCols` 가드를 지우지 마라.
- `[ ]` **`getLastRow()`·`getLastColumn()` 은 같은 실행에서 방금 쓴 것을 못 볼 수 있다.**
  쓰기는 실행이 끝날 때 커밋된다. 방금 만든 것과 비교해 더 큰 쪽을 쓰거나 `flush()` 하라.
- `[ ]` **인덱스·수업 JSON 은 캐시된다.** 인덱스 시트를 새로 고치는 코드를 추가하면
  `invalidateIndexCache(code)` 를, 수업을 고치면 `writeLesson` 을 거쳐라 —
  안 그러면 **옛 편집키·옛 현황암호가 최대 5분 동안 통한다.**
- `[ ]` **왕복 횟수를 줄여라. 한 왕복 1.2초는 Apps Script 의 바닥값**이라 캐시로 못 내린다
  (2026-08-20 실측). 문항마다 부르지 말고 `gradeAnswers` 처럼 묶어라.

## 디렉터리 지도

전체 파일 구조는 `docs/PLAN.md`의 「파일 구조」 절 참고. 요약:
- `src/types/lesson.ts` — 전 계층 공유 데이터 모델
- `src/api/mock.ts` / `client.ts` — localStorage 목 백엔드 / 실제 Apps Script 클라이언트 (`VITE_API_MODE`로 전환)
- `src/blocks/` — 블록·문항 레지스트리와 컴포넌트
- `src/lib/` — 순수 함수 (채점·검증·마이그레이션 등)
- `apps-script/Code.gs` — 백엔드 전체 (단일 `doPost` 라우터)
- `docs/` — 계획·진행상황·결정기록·운영정보 (아래 참고)

## 개발 명령

```
npm install
npm run dev          # VITE_API_MODE=mock 로 로컬 개발
npm run typecheck    # tsc --noEmit
npm run test         # vitest (순수 함수 단위 테스트)
npm run build         # GitHub Pages용 빌드
```

## 문서 지도 — 인계 체계

**사용자는 아래 표의 문서들(이 `CLAUDE.md` 포함)을 통틀어 "인수인계 파일"이라고 부른다.** "인수인계 파일 확인해/갱신해" 같은 지시는 이 표 전체를 뜻한다 — 어느 한 파일만 열어보고 끝내지 말 것.

| 파일 | 언제 열어보나 |
|---|---|
| `CLAUDE.md` | 이 파일. 규칙·구조·용어("인수인계 파일" 지칭 포함) |
| `docs/PROGRESS.md` | **항상 가장 먼저.** 현재 단계·다음 할 일·미해결 이슈 |
| `docs/PLAN.md` | 설계를 왜 이렇게 했는지 전체 맥락이 필요할 때 |
| `docs/구현계획서.docx` | PLAN.md의 Word 사본(사람이 읽는 용도). **직접 고치지 말 것** — PLAN.md를 고치고 재변환한다 |
| `docs/DECISIONS.md` | "이거 왜 이렇게 했지?"가 궁금할 때, 새 결정을 내렸을 때 |
| `docs/ROADMAP.md` | 다음에 뭘 만들지 고민될 때 — 경쟁 서비스 벤치마킹 기반 기능 제안 + 실사용 리스크 대응 계획(채택/보류 항목 구분) |
| `docs/SESSION_LOG.md` | 지난 세션에 뭘 했는지 |
| `docs/OPERATIONS.md` | Apps Script 배포 URL, Drive 폴더, 배포·장애 대응 절차 |
| `apps-script/SETUP.md` | 백엔드를 처음 배포하거나 재배포할 때 — **아래 절차 요약 참고** |
| `docs/TEACHER_GUIDE.md` | 교사용 사용법 (기능이 실제 사용자에게 어떻게 보이는지 확인할 때) |

**작업을 끝낼 때마다**: `docs/PROGRESS.md` 갱신 → 새 결정이 있으면 `docs/DECISIONS.md`에 이유 기록 → 커밋(메시지 첫 줄에 단계 번호, 예: `[4] 기본 문항 6종 구현`) → 푸시. **이 저장소에서는 커밋·푸시를 매번 물어보지 않고 바로 한다** — 사용자가 2026-07-29에 명시적으로 지시("커밋은 항상 묻지말고 해"), 일반적인 "커밋은 사용자가 명시적으로 요청할 때만" 원칙에 대한 이 프로젝트 한정 사전 승인이다. 단, `--force` 푸시·`git reset --hard`·기존 커밋 amend 같은 되돌리기 어려운 조작은 이 승인에 안 포함되니 여전히 그때그때 확인받을 것.

### Apps Script(`Code.gs`) 재배포 절차 — 항상 이 순서로 할 것 (2026-07-29 확정, 같은 날 배포 단계까지 어시스턴트 수행으로 갱신)

**"재배포"는 파일 업로드가 아니라 브라우저 에디터에 복사-붙여넣기다.** 이 프로젝트는 `clasp` 같은 CLI 배포 도구를 쓰지 않는다(의도적 — `apps-script/SETUP.md`가 요구하는 건 전부 브라우저 조작). **어시스턴트가 claude-in-chrome으로 1~4단계(저장뿐 아니라 배포 클릭까지) 전부 수행한다** — 사용자가 "앞으로 항상 내가 배포까지 수행"으로 명시적으로 위임(2026-07-29, 배포가 공유 중앙 백엔드에 즉시 영향을 준다는 점을 미리 설명하고 받은 승인). 5단계(스크립트 속성 등록)만 계정 설정 화면 진입이 필요해 필요시에만 사용자에게 넘긴다.

0. **[어시스턴트가 함]** ⚠️ **확장이 "연결되지 않음"이면 먼저 Chrome이 떠 있는지 본다.** 2026-08-18에 확장 문제로 오해해 배포를 포기했는데, 실제로는 **Chrome이 아예 실행 중이 아니었다.** `Start-Process chrome`으로 띄우면 바로 붙는다.
1. **[어시스턴트가 함]** [script.google.com](https://script.google.com)에서 기존 "InteractiveClass Backend" 프로젝트를 연다. `script.google.com/home/my` 목록에서 이름으로 찾아 **더블클릭**해서 들어간다(단일 클릭은 안 열림 — Drive 파일 목록과 같은 UX).
2. **[어시스턴트가 함]** `Code.gs` 내용을 리포지토리의 `apps-script/Code.gs`로 통째로 교체한다. **정확한 방법(아래 순서를 지킬 것 — 다른 방법은 실패했다, `docs/DECISIONS.md` 2026-07-29 항목에 실패 사례 기록):**
   - 왼쪽 파일 목록에서 `Code.gs` 파일 탭을 클릭해 실제로 포커스된 활성 에디터로 만든다(`monaco.editor.getEditors()[0]`로 포커스된 에디터가 맞는지 확인 가능).
   - `model.setValue(...)`가 아니라 **`editor.executeEdits('replace', [{range: model.getFullModelRange(), text: 새내용}])`** 로 교체한다 — 이게 실제 사용자 입력과 같은 경로로 이벤트가 발생해 앱의 "저장 필요" 상태가 제대로 갱신된다(`setValue`만 쓰면 화면엔 반영되지만 저장이 씹힌다).
   - 파일이 커서(900줄대) automation 스크립트 문자열에 직접 박아 넣으면 문제가 생길 수 있으니, 로컬에 임시 정적 HTTP 서버(Node `http` 모듈, CORS 헤더 포함)를 띄우고 페이지 안에서 `fetch`로 가져와 쓴다. 작업 끝나면 그 서버는 반드시 종료한다.
3. **[어시스턴트가 함]** 저장은 **"Drive에 프로젝트 저장" 버튼을 클릭하지 말고, 에디터에 포커스가 있는 상태에서 `computer` 툴의 `key` 액션으로 `ctrl+s`를 누른다**
   - ⚠️ **"에디터에 포커스"가 핵심이고, `computer` 클릭만으로는 포커스가 안 잡힌다**(2026-08-06에 원인 확인 — 클릭은 정확히 `.lines-content`에 꽂히는데도 `hasTextFocus()`가 계속 `false`). **`javascript_tool`로 `document.querySelector('.monaco-editor textarea').focus()`를 먼저 부른 뒤** `ctrl+s`를 보내면 한 번에 저장된다. 이 순서를 지키면 아래의 "첫 시도 실패" 재시도 루프를 대부분 건너뛸 수 있다. 저장이 걸렸는지는 화면에 `저장 중...`이 뜨는지로 즉시 확인 가능. — 저장 버튼 클릭은 이 세션에서 두 번 실패했다(반영이 안 됨). `ctrl+s` 직후 곧바로 새로고침하면 "저장 중..."이 끝나기 전이라 반영이 안 된 것처럼(구버전으로) 보일 수 있었다 — `ctrl+s` 후 2~3초는 기다렸다가 새로고침할 것. 저장했다고 믿지 말고, **반드시 페이지를 새로고침한 뒤 모델 내용을 로컬 파일과 문자열 그대로 비교**해서(`remote === local`) 실제로 서버에 반영됐는지 검증한다. `false`가 나오면 실패로 간주하고 파일 탭 클릭부터 다시 반복할 것(재시도하면 대개 된다) — 검증 전엔 다음 단계로 넘어가지 않는다. 이 저장 재시도 패턴은 2026-07-29 세션 4에서 두 번의 별도 배포 모두 첫 시도 실패 → 재시도 성공으로 재현됐다 — 항상 있을 수 있는 일로 취급할 것.
4. **[어시스턴트가 함, 2026-07-29부터]** 우상단 **배포 → 배포 관리(Manage deployments)** → 기존 배포 옆 연필(✏️) 아이콘 → 버전을 **새 버전(New version)** 으로 바꾸고 **배포** 버튼까지 클릭한다.
   - ⚠️ **"새 배포"를 새로 만들면 안 된다** — 웹앱 URL이 바뀌어서 `.env.local`과 GitHub 리포 Variables(`VITE_APPS_SCRIPT_URL`)까지 같이 고쳐야 하는 불필요한 후속 작업이 생긴다. 반드시 **기존 배포를 편집**해 새 버전으로 올려야 URL이 그대로 유지된다.
   - 배포는 공유 중앙 백엔드에 **즉시** 영향을 준다(다른 교사·학생이 그 순간 쓰고 있을 수 있음) — 3단계 저장 검증(`remote === local`)이 확실히 끝난 뒤에만 이 단계로 넘어갈 것. 배포 직후 아래 "배포 완료 후 재확인"으로 실제 반영을 확인하고, 이상이 있으면 즉시 사용자에게 알린다.
5. **[사용자만 할 수 있음, 필요시]** 관리자 화면(`/#/admin`)을 쓰려면 이것과 별개로 **프로젝트 설정 → 스크립트 속성**에 `ADMIN_PASSWORD`를 등록해야 한다 — 이건 코드 배포가 아니라 설정값이고, 계정 보안 설정 화면이라 어시스턴트가 대신 하지 않는다. 이미 등록되어 있다면(현재 상태) 다시 할 필요 없다.

**배포(4번) 완료 후 재확인**: `/#/admin`에 아무 비밀번호나 제출해 `"알 수 없는 action: listLessons"` 에러가 사라졌는지(사라졌으면 4번 완료, 안 사라졌으면 아직) 확인하는 게 가장 빠르다. 새 액션을 추가한 배포라면 그 액션을 실제로 호출해보는 것까지 확인할 것(예: 관리자 화면에서 새로 추가한 버튼을 실클릭).

**되돌리기**: 배포 후 문제가 발견되면 배포 관리 화면에서 같은 배포를 다시 편집해 **직전 버전 번호**로 되돌리고 배포하면 된다(Apps Script는 버전을 계속 누적하므로 이전 버전이 남아있다). 이 되돌리기도 어시스턴트가 수행 가능 — 단, 실행 전 사용자에게 상황을 알린다.

### 프런트엔드(GitHub Pages) 배포 확인

`main`에 push하면 GitHub Actions가 자동으로 빌드·배포한다(보통 1분 안팎).
**확인 절차는 공통 규칙 5번**을 그대로 따른다 — 이 앱은 번들 파일명이 바뀌므로
`curl -s https://chan7881.github.io/class/ | grep -o 'assets/index-[A-Za-z0-9_-]*\.js'`로
배포 전후 해시를 비교하는 것이 가장 확실하다.
