# 이 파일을 복사해 .env.local 로 만들고 값을 채운다. .env.local 은 절대 커밋하지 않는다(.gitignore 처리됨).

# api/mock.ts (localStorage) 대신 실제 Apps Script 백엔드를 쓸지 여부
VITE_API_MODE=mock

# Apps Script 웹앱 배포 URL — apps-script/SETUP.md대로 배포한 뒤 여기에 넣는다.
# 형식: https://script.google.com/macros/s/XXXXXXXX/exec
VITE_APPS_SCRIPT_URL=
